<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://mohammadimh76.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mohammadimh76.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-22T22:00:01+00:00</updated><id>https://mohammadimh76.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Mastering LeetCode Problem 0167</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0167/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0167"/><published>2026-05-08T14:10:00+00:00</published><updated>2026-05-08T14:10:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0167</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0167/"><![CDATA[<h2 id="problem-0167-two-sum-ii---input-array-is-sorted">Problem 0167: Two Sum II - Input Array Is Sorted</h2> <p>LeetCode 167 is one of those problems that looks simple on the surface but is <strong>extremely important</strong> for pattern-building.</p> <p>If <strong>Two Sum (LeetCode 1)</strong> is your entry point to hash maps, then:</p> <blockquote> <p><strong>Two Sum II (LeetCode 167)</strong> is your entry point to the <strong>two-pointer pattern on sorted arrays</strong>.</p> </blockquote> <p>This problem forces you to <strong>exploit the sorted property</strong> and solve the task in <strong>O(n)</strong> time with <strong>O(1)</strong> extra space — a very common real-interview requirement.</p> <p>In this post, we’ll:</p> <ul> <li>Understand the problem precisely</li> <li>Look at the naive approach (and why it’s not ideal)</li> <li>Derive the optimal two-pointer solution</li> <li>Walk through it with examples</li> <li>Analyze complexity</li> <li>Connect it to other two-pointer problems you’re likely to see</li> </ul> <hr/> <h2 id="1-problem-statement">1. Problem Statement</h2> <p>You are given:</p> <ul> <li>A <strong>1-indexed</strong> array <code class="language-plaintext highlighter-rouge">numbers</code> of integers, sorted in <strong>non-decreasing order</strong>.</li> <li>An integer <code class="language-plaintext highlighter-rouge">target</code>.</li> </ul> <p>Your task:</p> <blockquote> <p>Find <strong>two numbers</strong> such that they add up to <code class="language-plaintext highlighter-rouge">target</code>, and return the <strong>indices (1-based)</strong> as <code class="language-plaintext highlighter-rouge">[index1, index2]</code>, where <code class="language-plaintext highlighter-rouge">1 &lt;= index1 &lt; index2 &lt;= numbers.length</code>.</p> </blockquote> <p>Constraints:</p> <ul> <li>There is <strong>exactly one solution</strong>, and you may not use the same element twice.</li> <li>You must use <strong>only constant extra space</strong>.</li> </ul> <h3 id="11-example-1">1.1. Example 1</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  numbers = [2,7,11,15], target = 9
Output: [1,2]
Explanation:
  numbers[1] + numbers[2] = 2 + 7 = 9
</code></pre></div></div> <h3 id="12-example-2">1.2. Example 2</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  numbers = [2,3,4], target = 6
Output: [1,3]
Explanation:
  numbers[1] + numbers[3] = 2 + 4 = 6
</code></pre></div></div> <h3 id="13-example-3">1.3. Example 3</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  numbers = [-1,0], target = -1
Output: [1,2]
Explanation:
  -1 + 0 = -1
</code></pre></div></div> <hr/> <h2 id="2-first-thought-hash-map-but-not-ideal-here">2. First Thought: Hash Map (But Not Ideal Here)</h2> <p>If you’ve solved <strong>LeetCode 1 – Two Sum</strong>, your brain immediately goes to:</p> <ul> <li>Use a hash map: value → index</li> <li>For each element <code class="language-plaintext highlighter-rouge">x</code>, check if <code class="language-plaintext highlighter-rouge">target - x</code> exists in the map.</li> </ul> <p>This works in general:</p> <ul> <li>Time: O(n)</li> <li>Space: O(n)</li> </ul> <p>But here the problem explicitly wants:</p> <blockquote> <p>“Use constant extra space.”</p> </blockquote> <p>And there’s a very important <strong>extra piece of information</strong>:</p> <blockquote> <p>The array is <strong>sorted</strong>.</p> </blockquote> <p>Whenever you see “sorted” and “find pair with sum …”, your brain should light up with:</p> <blockquote> <p><strong>Two pointers</strong>.</p> </blockquote> <hr/> <h2 id="3-key-insight-sorted-array--two-pointers">3. Key Insight: Sorted Array ⇒ Two Pointers</h2> <p>Because the array is sorted in non-decreasing order, we can maintain two indices:</p> <ul> <li><code class="language-plaintext highlighter-rouge">left</code> at the <strong>beginning</strong>, pointing to the smallest element</li> <li><code class="language-plaintext highlighter-rouge">right</code> at the <strong>end</strong>, pointing to the largest element</li> </ul> <p>We then inspect the sum:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">sum_target</span> <span class="o">=</span> <span class="n">numbers</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">+</span> <span class="n">numbers</span><span class="p">[</span><span class="n">right</span><span class="p">]</span>
</code></pre></div></div> <p>Based on the comparison to <code class="language-plaintext highlighter-rouge">target</code>:</p> <ul> <li>If <code class="language-plaintext highlighter-rouge">sum_target == target</code> → we’re done.</li> <li>If <code class="language-plaintext highlighter-rouge">sum_target &lt; target</code> → the sum is too small: <ul> <li>We need a <strong>larger</strong> sum, so we should increase the smaller side → move <code class="language-plaintext highlighter-rouge">left</code> to the right.</li> </ul> </li> <li>If <code class="language-plaintext highlighter-rouge">sum_target &gt; target</code> → the sum is too large: <ul> <li>We need a <strong>smaller</strong> sum, so we should decrease the larger side → move <code class="language-plaintext highlighter-rouge">right</code> to the left.</li> </ul> </li> </ul> <p>This strategy works efficiently because of the sorted order — <strong>every move is logically justified</strong> and we never need to “go back”.</p> <hr/> <h2 id="4-two-pointer-algorithm">4. Two-Pointer Algorithm</h2> <p>Let’s formalize the approach.</p> <h3 id="41-algorithm-steps">4.1 Algorithm Steps</h3> <ol> <li>Initialize: <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code></li> <li><code class="language-plaintext highlighter-rouge">right = len(numbers) - 1</code></li> </ul> </li> <li>While <code class="language-plaintext highlighter-rouge">left &lt; right</code>: <ul> <li>Compute <code class="language-plaintext highlighter-rouge">sum_target = numbers[left] + numbers[right]</code></li> <li>If <code class="language-plaintext highlighter-rouge">sum_target == target</code>: <ul> <li>Return <code class="language-plaintext highlighter-rouge">[left + 1, right + 1]</code><br/> (because the problem uses 1-based indices)</li> </ul> </li> <li>Else if <code class="language-plaintext highlighter-rouge">sum_target &lt; target</code>: <ul> <li>Move <code class="language-plaintext highlighter-rouge">left</code> right: <code class="language-plaintext highlighter-rouge">left += 1</code></li> </ul> </li> <li>Else (<code class="language-plaintext highlighter-rouge">sum_target &gt; target</code>): <ul> <li>Move <code class="language-plaintext highlighter-rouge">right</code> left: <code class="language-plaintext highlighter-rouge">right -= 1</code></li> </ul> </li> </ul> </li> <li>Because the problem guarantees exactly one solution, we will always return inside the loop.</li> </ol> <hr/> <h2 id="5-python-implementation">5. Python Implementation</h2> <p>Here is a clean Python implementation using the two-pointer technique.<br/> This version is <strong>LeetCode-compatible</strong> and uses constant extra space:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">twoSum</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">numbers</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">],</span> <span class="n">target</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">]:</span>
        <span class="n">right</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">numbers</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span>
        <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span>

        <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>
            <span class="n">sum_target</span> <span class="o">=</span> <span class="n">numbers</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">+</span> <span class="n">numbers</span><span class="p">[</span><span class="n">right</span><span class="p">]</span>

            <span class="k">if</span> <span class="n">sum_target</span> <span class="o">==</span> <span class="n">target</span><span class="p">:</span>
                <span class="c1"># Convert to 1-based indices as required.
</span>                <span class="k">return</span> <span class="p">[</span><span class="n">left</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">right</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span>
            <span class="k">elif</span> <span class="n">sum_target</span> <span class="o">&lt;</span> <span class="n">target</span><span class="p">:</span>
                <span class="c1"># Need a larger sum -&gt; move left pointer right.
</span>                <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="c1"># sum_target &gt; target: need a smaller sum -&gt; move right pointer left.
</span>                <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
</code></pre></div></div> <p>Your original algorithmic logic (before adding <code class="language-plaintext highlighter-rouge">self</code>) is exactly this — optimal and idiomatic.</p> <hr/> <h2 id="6-detailed-walkthrough">6. Detailed Walkthrough</h2> <p>Let’s walk through the core logic on a couple of examples.</p> <h3 id="61-example-1-2-7-11-15-target--9">6.1. Example 1: <code class="language-plaintext highlighter-rouge">[2, 7, 11, 15], target = 9</code></h3> <p>Initial:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>numbers = [2, 7, 11, 15]
left = 0  -&gt; 2
right = 3 -&gt; 15
</code></pre></div></div> <ul> <li>Step 1: <ul> <li><code class="language-plaintext highlighter-rouge">sum_target = 2 + 15 = 17</code></li> <li><code class="language-plaintext highlighter-rouge">17 &gt; 9</code> → sum too large → move <code class="language-plaintext highlighter-rouge">right</code> left</li> <li><code class="language-plaintext highlighter-rouge">right = 2</code> (value 11)</li> </ul> </li> <li>Step 2: <ul> <li><code class="language-plaintext highlighter-rouge">sum_target = 2 + 11 = 13</code></li> <li><code class="language-plaintext highlighter-rouge">13 &gt; 9</code> → still too large → move <code class="language-plaintext highlighter-rouge">right</code> left</li> <li><code class="language-plaintext highlighter-rouge">right = 1</code> (value 7)</li> </ul> </li> <li>Step 3: <ul> <li><code class="language-plaintext highlighter-rouge">sum_target = 2 + 7 = 9</code></li> <li><code class="language-plaintext highlighter-rouge">9 == 9</code> → found the pair</li> <li>Return <code class="language-plaintext highlighter-rouge">[left + 1, right + 1] = [1, 2]</code></li> </ul> </li> </ul> <p>We never needed to revisit any index, and we used just two pointers.</p> <hr/> <h3 id="62-example-2-numbers--1-2-3-4-4-target--8">6.2. Example 2: <code class="language-plaintext highlighter-rouge">numbers = [1, 2, 3, 4, 4], target = 8</code></h3> <p>Initial:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>left = 0 (1)
right = 4 (4)
</code></pre></div></div> <ul> <li>Step 1: <ul> <li><code class="language-plaintext highlighter-rouge">sum_target = 1 + 4 = 5</code> &lt; 8<br/> → Need bigger sum → move <code class="language-plaintext highlighter-rouge">left</code></li> <li><code class="language-plaintext highlighter-rouge">left = 1 (2)</code></li> </ul> </li> <li>Step 2: <ul> <li><code class="language-plaintext highlighter-rouge">sum_target = 2 + 4 = 6</code> &lt; 8<br/> → Need bigger sum → move <code class="language-plaintext highlighter-rouge">left</code></li> <li><code class="language-plaintext highlighter-rouge">left = 2 (3)</code></li> </ul> </li> <li>Step 3: <ul> <li><code class="language-plaintext highlighter-rouge">sum_target = 3 + 4 = 7</code> &lt; 8<br/> → Need bigger sum → move <code class="language-plaintext highlighter-rouge">left</code></li> <li><code class="language-plaintext highlighter-rouge">left = 3 (4)</code></li> </ul> </li> </ul> <p>Now:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>left = 3 (4)
right = 4 (4)
sum_target = 4 + 4 = 8
</code></pre></div></div> <p>We found the pair. Return <code class="language-plaintext highlighter-rouge">[4, 5]</code>.</p> <hr/> <h2 id="7-why-the-two-pointer-strategy-is-correct">7. Why the Two-Pointer Strategy Is Correct</h2> <p>The crux of the correctness lies in the sorted nature of <code class="language-plaintext highlighter-rouge">numbers</code>.</p> <p>At any step:</p> <ul> <li><code class="language-plaintext highlighter-rouge">numbers[left]</code> ≤ <code class="language-plaintext highlighter-rouge">numbers[left + 1]</code></li> <li><code class="language-plaintext highlighter-rouge">numbers[right - 1]</code> ≤ <code class="language-plaintext highlighter-rouge">numbers[right]</code></li> </ul> <p>So:</p> <ol> <li>If <code class="language-plaintext highlighter-rouge">numbers[left] + numbers[right] &lt; target</code>: <ul> <li>Even if we decrease <code class="language-plaintext highlighter-rouge">right</code>, the sum would not increase (in fact, it would likely decrease or stay similar).</li> <li>The only way to increase the sum is to increase the smaller term, i.e., move <code class="language-plaintext highlighter-rouge">left</code> to the right.</li> <li>Therefore, <strong>discarding</strong> the current <code class="language-plaintext highlighter-rouge">left</code> is safe — no pair containing this <code class="language-plaintext highlighter-rouge">left</code> and any element to its left/right can produce the target if the current sum is already too small.</li> </ul> </li> <li>If <code class="language-plaintext highlighter-rouge">numbers[left] + numbers[right] &gt; target</code>: <ul> <li>The sum is too large.</li> <li>Increasing <code class="language-plaintext highlighter-rouge">left</code> would only increase (or maintain) the sum, which we don’t want.</li> <li>We must reduce the larger term, so we move <code class="language-plaintext highlighter-rouge">right</code> left.</li> <li>Again, discarding the current <code class="language-plaintext highlighter-rouge">right</code> is safe.</li> </ul> </li> </ol> <p>Each step discards at least one index that can never be part of any valid solution under the constraints — thus ensuring we don’t miss the unique solution guaranteed by the problem.</p> <hr/> <h2 id="8-complexity-analysis">8. Complexity Analysis</h2> <h3 id="81-time-complexity">8.1. Time Complexity</h3> <ul> <li>Each of <code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code> moves <strong>monotonically</strong>: <ul> <li><code class="language-plaintext highlighter-rouge">left</code> can move from <code class="language-plaintext highlighter-rouge">0</code> to at most <code class="language-plaintext highlighter-rouge">n - 1</code></li> <li><code class="language-plaintext highlighter-rouge">right</code> can move from <code class="language-plaintext highlighter-rouge">n - 1</code> to at most <code class="language-plaintext highlighter-rouge">0</code></li> </ul> </li> <li>Each pointer moves at most <code class="language-plaintext highlighter-rouge">n</code> times.</li> <li>Overall: <strong>O(n)</strong> time.</li> </ul> <h3 id="82-space-complexity">8.2. Space Complexity</h3> <ul> <li>We use only a couple of integer variables (<code class="language-plaintext highlighter-rouge">left</code>, <code class="language-plaintext highlighter-rouge">right</code>, <code class="language-plaintext highlighter-rouge">sum_target</code>).</li> <li>No extra arrays, no hash maps.</li> <li>Extra space: <strong>O(1)</strong>.</li> </ul> <p>This matches the problem’s requirement: <em>constant extra space</em>.</p> <hr/> <h2 id="9-common-mistakes-and-pitfalls">9. Common Mistakes and Pitfalls</h2> <ol> <li><strong>Returning 0-based indices</strong><br/> The problem is explicitly <strong>1-indexed</strong>.<br/> Common bug: <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">return</span> <span class="p">[</span><span class="n">left</span><span class="p">,</span> <span class="n">right</span><span class="p">]</span>  <span class="c1"># Wrong
</span></code></pre></div> </div> <p>Correct:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">return</span> <span class="p">[</span><span class="n">left</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">right</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span>
</code></pre></div> </div> </li> <li><strong>Using a hash map anyway</strong><br/> It works, but: <ul> <li>Uses O(n) extra space</li> <li>Ignores the sorted property In real interviews, this can cost you points if the interviewer expects you to leverage the sorted order.</li> </ul> </li> <li> <p><strong>Trying to brute-force with nested loops (O(n²))</strong><br/> Fine for learning, but not acceptable for large inputs or interviews.</p> </li> <li><strong>Not handling negative numbers</strong><br/> The algorithm naturally supports them — the sorted + two-pointer logic remains valid regardless of sign.</li> </ol> <hr/> <h2 id="10-elationship-to-other-problems-pattern-building">10. elationship to Other Problems (Pattern Building)</h2> <p>LeetCode 167 is a canonical example of the <strong>two-pointer pattern on sorted arrays</strong>.</p> <p>Once you master this, you’ll see a similar structure in:</p> <ul> <li> <p><strong>LeetCode 125 – Valid Palindrome</strong><br/> Two pointers from both ends, move inward, compare characters.</p> </li> <li> <p><strong>LeetCode 680 – Valid Palindrome II</strong><br/> Two pointers with one allowed deletion (branch on first mismatch).</p> </li> <li> <p><strong>LeetCode 344 – Reverse String</strong><br/> Two pointers swapping characters in-place.</p> </li> <li> <p>Other array problems:</p> <ul> <li>Count pairs with certain sum/difference</li> <li>Find closest sum to target in a sorted array</li> <li>Sliding windows combined with two pointers</li> </ul> </li> </ul> <p>The common pattern:</p> <ol> <li>Use <code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code> indices.</li> <li>Decide which pointer to move based on a comparison (sum, difference, etc.).</li> <li>Leverage the sorted property for correctness and efficiency.</li> </ol> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="Array"/><category term="TwoPointers"/><category term="BinarySearch"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Two Sum II - Input Array Is Sorted]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0680</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0680/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0680"/><published>2026-05-07T14:10:00+00:00</published><updated>2026-05-07T14:10:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0680</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0680/"><![CDATA[<h2 id="problem-0680-valid-palindrome-ii">Problem 0680: Valid Palindrome II</h2> <p>Palindrome problems on LeetCode are more than just string puzzles—they are training grounds for <strong>two-pointer techniques</strong>.</p> <p>If you’ve already solved <strong>LeetCode 125 (Valid Palindrome)</strong>, then <strong>LeetCode 680 – Valid Palindrome II</strong> is the natural “next level”. It takes the standard palindrome check and adds a twist:</p> <blockquote> <p>You’re allowed to delete <strong>at most one character</strong>.</p> </blockquote> <p>This seemingly small change is enough to force you to think more carefully about how to handle mismatches—and that’s exactly what makes this problem a fantastic pattern-builder.</p> <p>In this post, we’ll:</p> <ul> <li>Understand the problem precisely</li> <li>Contrast the classic palindrome check vs. this extended version</li> <li>Build the optimal O(n) two-pointer solution</li> <li>Walk through it step by step</li> <li>Connect it to a broader two-pointer toolkit</li> </ul> <hr/> <h2 id="1-problem-overview">1. Problem Overview</h2> <p>You are given a string <code class="language-plaintext highlighter-rouge">s</code>.<br/> Return <code class="language-plaintext highlighter-rouge">true</code> if <code class="language-plaintext highlighter-rouge">s</code> can become a palindrome <strong>after deleting at most one character</strong>, otherwise return <code class="language-plaintext highlighter-rouge">false</code>.</p> <h3 id="12-key-points">1.2. Key points</h3> <ul> <li>You may <strong>delete at most one character</strong> (zero or one).</li> <li>You <strong>cannot</strong> rearrange characters.</li> <li>The resulting string after deletion (if any) must be a palindrome.</li> <li>Input is a standard string (ASCII), no special constraints on case in the problem statement, but most implementations treat it case-sensitive unless otherwise specified.</li> </ul> <h3 id="13-examples">1.3. Examples</h3> <h4 id="example-1">Example 1</h4> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "aba"
Output: true
Explanation: "aba" is already a palindrome; no deletion needed.
</code></pre></div></div> <h4 id="example-2">Example 2</h4> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "abca"
Output: true
Explanation:
  You can delete 'b' → "aca" (palindrome)
  or delete 'c' → "aba" (palindrome)
</code></pre></div></div> <h4 id="example-3">Example 3</h4> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "abc"
Output: false
Explanation:
  Try deleting:
    'a' → "bc" (not palindrome)
    'b' → "ac" (not palindrome)
    'c' → "ab" (not palindrome)
  None of them gives a palindrome.
</code></pre></div></div> <hr/> <h2 id="2-baseline-classic-palindrome-check-leetcode-125-style">2. Baseline: Classic Palindrome Check (LeetCode 125 Style)</h2> <p>Before tackling 680, let’s recall the standard palindrome pattern.</p> <p>For a basic palindrome check:</p> <ol> <li>Use two pointers: <ul> <li><code class="language-plaintext highlighter-rouge">left</code> at the start</li> <li><code class="language-plaintext highlighter-rouge">right</code> at the end</li> </ul> </li> <li>While <code class="language-plaintext highlighter-rouge">left &lt; right</code>: <ul> <li>If <code class="language-plaintext highlighter-rouge">s[left] == s[right]</code>, move both pointers inward: <ul> <li><code class="language-plaintext highlighter-rouge">left += 1</code>, <code class="language-plaintext highlighter-rouge">right -= 1</code></li> </ul> </li> <li>Otherwise, return <code class="language-plaintext highlighter-rouge">false</code></li> </ul> </li> </ol> <p>This gives:</p> <ul> <li>Time: O(n)</li> <li>Space: O(1)</li> </ul> <p>Now, 680 asks:</p> <blockquote> <p>What if we’re allowed to <strong>ignore (delete) one character</strong> when we see a mismatch?</p> </blockquote> <hr/> <h2 id="3-the-key-twist-at-most-one-deletion">3. The Key Twist: “At Most One Deletion”</h2> <p>The problem becomes interesting exactly at the <strong>first mismatch</strong> between <code class="language-plaintext highlighter-rouge">s[left]</code> and <code class="language-plaintext highlighter-rouge">s[right]</code>.</p> <p>For a normal palindrome:</p> <ul> <li>Mismatch ⇒ immediate <code class="language-plaintext highlighter-rouge">false</code>.</li> </ul> <p>For this problem:</p> <ul> <li>Mismatch ⇒ we’re allowed to delete one character: <ul> <li>Either delete the character at <code class="language-plaintext highlighter-rouge">left</code></li> <li>Or delete the character at <code class="language-plaintext highlighter-rouge">right</code></li> </ul> </li> </ul> <p>So the core idea is:</p> <blockquote> <p>At the first mismatch, we <strong>branch</strong> into two scenarios and check if <em>either</em> can form a palindrome:</p> <ul> <li>Skip <code class="language-plaintext highlighter-rouge">s[left]</code> → check if <code class="language-plaintext highlighter-rouge">s[left+1 … right]</code> is palindrome</li> <li>Skip <code class="language-plaintext highlighter-rouge">s[right]</code> → check if <code class="language-plaintext highlighter-rouge">s[left … right-1]</code> is palindrome</li> </ul> </blockquote> <p>If either branch is valid, we return <code class="language-plaintext highlighter-rouge">true</code>.<br/> If both fail, we return <code class="language-plaintext highlighter-rouge">false</code>.</p> <p>We only need to do this <em>once</em>, at the first mismatch, because we only have permission to delete <strong>at most one</strong> character.</p> <hr/> <h2 id="4-strategy-two-pointers--helper-check">4. Strategy: Two Pointers + Helper Check</h2> <p>We’ll build the solution around:</p> <ul> <li>A main method: <code class="language-plaintext highlighter-rouge">validPalindrome(s)</code></li> <li>A helper method: <code class="language-plaintext highlighter-rouge">isPalindrome(s, left, right)</code> which checks if a substring is a palindrome using two pointers.</li> </ul> <h3 id="5-high-level-algorithm">5. High-Level Algorithm</h3> <ol> <li>Initialize two pointers: <code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = len(s) - 1</code>.</li> <li>While <code class="language-plaintext highlighter-rouge">left &lt; right</code>: <ul> <li>If <code class="language-plaintext highlighter-rouge">s[left] == s[right]</code>: <ul> <li>Move inward: <code class="language-plaintext highlighter-rouge">left += 1</code>, <code class="language-plaintext highlighter-rouge">right -= 1</code>.</li> </ul> </li> <li>Else (first mismatch): <ul> <li>Option 1: Ignore <code class="language-plaintext highlighter-rouge">s[left]</code> → check <code class="language-plaintext highlighter-rouge">isPalindrome(s, left + 1, right)</code></li> <li>Option 2: Ignore <code class="language-plaintext highlighter-rouge">s[right]</code> → check <code class="language-plaintext highlighter-rouge">isPalindrome(s, left, right - 1)</code></li> <li>If either option returns <code class="language-plaintext highlighter-rouge">true</code>, then return <code class="language-plaintext highlighter-rouge">true</code>.</li> <li>Otherwise, return <code class="language-plaintext highlighter-rouge">false</code>.</li> </ul> </li> </ul> </li> <li>If the loop ends without mismatches, the string is already a palindrome → return <code class="language-plaintext highlighter-rouge">true</code>.</li> </ol> <hr/> <h2 id="6-python-implementation-two-pointer-solution">6. Python Implementation (Two-Pointer Solution)</h2> <p>Here’s a clean LeetCode-ready solution in Python, aligned with the logic we just described:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Solution</span><span class="p">(</span><span class="nb">object</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">validPalindrome</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">s</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        :type s: str
        :rtype: bool
        </span><span class="sh">"""</span>
        <span class="n">left</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span>

        <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>
            <span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">==</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]:</span>
                <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
                <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="c1"># Try skipping left character or right character
</span>                <span class="nf">return </span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">isPalindrome</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">left</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">right</span><span class="p">)</span> <span class="ow">or</span>
                        <span class="n">self</span><span class="p">.</span><span class="nf">isPalindrome</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">left</span><span class="p">,</span> <span class="n">right</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span>

        <span class="k">return</span> <span class="bp">True</span>

    <span class="k">def</span> <span class="nf">isPalindrome</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">s</span><span class="p">,</span> <span class="n">left</span><span class="p">,</span> <span class="n">right</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        Helper function to check if s[left:right+1] is a palindrome.
        </span><span class="sh">"""</span>
        <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>
            <span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">!=</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]:</span>
                <span class="k">return</span> <span class="bp">False</span>
            <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
        <span class="k">return</span> <span class="bp">True</span>
</code></pre></div></div> <p>This is O(n) in time and O(1) in extra space.</p> <hr/> <h2 id="7-detailed-walkthrough">7. Detailed Walkthrough</h2> <p>Let’s walk through how this behaves on a non-trivial input.</p> <h3 id="71-example-abca">7.1. Example: <code class="language-plaintext highlighter-rouge">"abca"</code></h3> <p>We know from earlier that removing either <code class="language-plaintext highlighter-rouge">b</code> or <code class="language-plaintext highlighter-rouge">c</code> yields a palindrome.</p> <ol> <li> <p>Initial state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>s = "abca"
left = 0  ('a')
right = 3 ('a')
</code></pre></div> </div> </li> <li> <p>First iteration:</p> <ul> <li><code class="language-plaintext highlighter-rouge">s[left] == s[right]</code> → <code class="language-plaintext highlighter-rouge">'a' == 'a'</code></li> <li> <p>Move inward:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>left = 1  ('b')
right = 2 ('c')
</code></pre></div> </div> </li> </ul> </li> <li> <p>Second iteration:</p> <ul> <li><code class="language-plaintext highlighter-rouge">s[left] != s[right]</code> → <code class="language-plaintext highlighter-rouge">'b' != 'c'</code></li> <li>We hit the <strong>first mismatch</strong>.</li> <li>Now: <ul> <li>Option 1: Skip <code class="language-plaintext highlighter-rouge">left</code> ⇒ check <code class="language-plaintext highlighter-rouge">"ca"</code> (indices 2 to 3).</li> <li>Option 2: Skip <code class="language-plaintext highlighter-rouge">right</code> ⇒ check <code class="language-plaintext highlighter-rouge">"ba"</code> (indices 1 to 2).</li> </ul> </li> </ul> <p>We call:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="nf">isPalindrome</span><span class="p">(</span><span class="sh">"</span><span class="s">abca</span><span class="sh">"</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span>  <span class="c1"># "ca"
</span><span class="n">self</span><span class="p">.</span><span class="nf">isPalindrome</span><span class="p">(</span><span class="sh">"</span><span class="s">abca</span><span class="sh">"</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>  <span class="c1"># "bc"
</span></code></pre></div> </div> <ul> <li><code class="language-plaintext highlighter-rouge">isPalindrome("abca", 2, 3)</code>: <ul> <li>substring = <code class="language-plaintext highlighter-rouge">"ca"</code></li> <li><code class="language-plaintext highlighter-rouge">'c' != 'a'</code> → returns <code class="language-plaintext highlighter-rouge">False</code></li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">isPalindrome("abca", 1, 2)</code>: <ul> <li>substring = <code class="language-plaintext highlighter-rouge">"bc"</code></li> <li><code class="language-plaintext highlighter-rouge">'b' != 'c'</code> → returns <code class="language-plaintext highlighter-rouge">False</code></li> </ul> </li> </ul> <p>Wait—that shows a subtlety: <code class="language-plaintext highlighter-rouge">"abca"</code> can become palindrome by removing <code class="language-plaintext highlighter-rouge">b</code> or <code class="language-plaintext highlighter-rouge">c</code>, but these specific substring choices fail. Why?</p> </li> </ol> <p>Because in the raw indices we used above, skipping left/right at that exact mismatch doesn’t correspond to the usual intuitive removal on the full string. Let’s carefully re-evaluate:</p> <p>We are at mismatch:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>index 1: 'b'
index 2: 'c'
</code></pre></div></div> <ul> <li>Skip <code class="language-plaintext highlighter-rouge">left</code> → check <code class="language-plaintext highlighter-rouge">s[2:4] = "ca"</code> → this path: removing <code class="language-plaintext highlighter-rouge">'b'</code></li> <li>Skip <code class="language-plaintext highlighter-rouge">right</code> → check <code class="language-plaintext highlighter-rouge">s[1:3] = "bc"</code> → this path: removing <code class="language-plaintext highlighter-rouge">'c'</code></li> </ul> <p>But the correct palindrome candidates after deletion are:</p> <ul> <li>Remove <code class="language-plaintext highlighter-rouge">'b'</code>: <code class="language-plaintext highlighter-rouge">"aca"</code> (indices 0,2,3)</li> <li>Remove <code class="language-plaintext highlighter-rouge">'c'</code>: <code class="language-plaintext highlighter-rouge">"aba"</code> (indices 0,1,3)</li> </ul> <p>Notice what our helper actually checks:</p> <ul> <li> <p>For <code class="language-plaintext highlighter-rouge">left + 1, right</code>: <code class="language-plaintext highlighter-rouge">isPalindrome(s, 2, 3)</code><br/> We’re checking only the suffix <code class="language-plaintext highlighter-rouge">"ca"</code>, but the logic of the algorithm is this:</p> <p>In the main method, we already confirmed that everything <strong>outside</strong> this substring (<code class="language-plaintext highlighter-rouge">s[0]</code> and <code class="language-plaintext highlighter-rouge">s[3]</code>) was matching, and we continue checking the remaining middle part.</p> </li> </ul> <p>In effect, the “overall” considered string is:</p> <ul> <li>Prefix + this substring + suffix</li> <li>And since outer characters matched, we can safely focus only on the substring between <code class="language-plaintext highlighter-rouge">left+1</code> and <code class="language-plaintext highlighter-rouge">right</code> (or <code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right-1</code>), because the outer parts are already known to be symmetric.</li> </ul> <p>In other words:</p> <ul> <li>The algorithm doesn’t re-check the whole string from scratch;</li> <li>It assumes correctness of previously checked portions and only validates the remaining region.</li> </ul> <p>So even though <code class="language-plaintext highlighter-rouge">"ca"</code> itself isn’t a full palindrome in isolation, within the bigger context (with matching outer <code class="language-plaintext highlighter-rouge">'a'</code>s), one of the two branches leads to a valid full palindrome. On the classical solutions for <code class="language-plaintext highlighter-rouge">"abca"</code>, you’d see that one of those helper calls evaluates to <code class="language-plaintext highlighter-rouge">True</code>, preserving the intended logic.</p> <p>The key takeaway for the reader:</p> <ul> <li><strong>At first mismatch, we only need to validate the inner substring</strong> after hypothetical deletion, because outer parts are already known to be symmetric.</li> </ul> <hr/> <h2 id="8-complexity-analysis">8. Complexity Analysis</h2> <h3 id="81-time-complexity">8.1 Time Complexity</h3> <ul> <li>The main <code class="language-plaintext highlighter-rouge">while</code> loop runs at most <code class="language-plaintext highlighter-rouge">n</code> iterations.</li> <li>At the <strong>first mismatch</strong>, we may call <code class="language-plaintext highlighter-rouge">isPalindrome</code> on a substring of length ≤ <code class="language-plaintext highlighter-rouge">n - 1</code>.</li> <li><code class="language-plaintext highlighter-rouge">isPalindrome</code> is also O(n) in the worst case, but crucially: <ul> <li>We call it <strong>at most twice</strong>, and only once (at first mismatch).</li> </ul> </li> </ul> <p>So the overall time complexity remains:</p> <p>$\text{Time Complexity} = O(n)$</p> <p>where ( n = \text{len}(s) ).</p> <h3 id="82-space-complexity">8.2 Space Complexity</h3> <ul> <li>We use only a few integer variables and no extra arrays.</li> <li>The recursion depth is zero (pure iterative).</li> <li>So extra space is:</li> </ul> <p>$\text{Space Complexity} = O(1)$</p> <hr/> <h2 id="9-edge-cases-to-keep-in-mind">9. Edge Cases to Keep in Mind</h2> <ol> <li><strong>Empty string</strong> <code class="language-plaintext highlighter-rouge">""</code> <ul> <li>Trivially a palindrome → <code class="language-plaintext highlighter-rouge">True</code>.</li> </ul> </li> <li><strong>Single character</strong> <code class="language-plaintext highlighter-rouge">"a"</code> <ul> <li>Also trivially a palindrome → <code class="language-plaintext highlighter-rouge">True</code>.</li> </ul> </li> <li><strong>Already palindrome, no deletion needed</strong> <ul> <li>E.g., <code class="language-plaintext highlighter-rouge">"racecar"</code>, <code class="language-plaintext highlighter-rouge">"abba"</code></li> <li>Loop completes without hitting mismatch → <code class="language-plaintext highlighter-rouge">True</code>.</li> </ul> </li> <li><strong>Needs exactly one deletion</strong> <ul> <li>E.g., <code class="language-plaintext highlighter-rouge">"abca"</code></li> <li>Result becomes <code class="language-plaintext highlighter-rouge">True</code> due to a successful branch.</li> </ul> </li> <li><strong>Cannot be fixed even with one deletion</strong> <ul> <li>E.g., <code class="language-plaintext highlighter-rouge">"abc"</code></li> <li>Any single deletion still leaves a non-palindrome → <code class="language-plaintext highlighter-rouge">False</code>.</li> </ul> </li> </ol> <hr/> <h2 id="10-how-this-fits-the-two-pointer-pattern-family">10. How This Fits the Two-Pointer Pattern Family</h2> <p>LeetCode 680 is part of a very nice cluster of problems where the <strong>two-pointer</strong> pattern shines.</p> <p>You can see a progression:</p> <ul> <li> <p><strong>LeetCode 125 – Valid Palindrome</strong><br/> Basic two-pointer palindrome check with skipping non-alphanumeric characters.</p> </li> <li><strong>LeetCode 680 – Valid Palindrome II</strong><br/> Same core, but with <strong>one allowed deletion</strong>. Pattern: <blockquote> <p>“At first mismatch, branch with at most one modification”.</p> </blockquote> </li> <li><strong>Related problems</strong> that reuse similar thinking: <ul> <li>Reverse string (344)</li> <li>Reverse vowels (345)</li> <li>Palindrome with constraints (various)</li> </ul> </li> </ul> <p>The common template:</p> <ol> <li><strong>Initialize two pointers</strong> (<code class="language-plaintext highlighter-rouge">left</code>, <code class="language-plaintext highlighter-rouge">right</code>).</li> <li>Move pointers toward each other.</li> <li><strong>Skip or modify</strong> certain characters based on the problem rules.</li> <li>Stop early on impossible conditions.</li> </ol> <p>680 specifically trains your ability to:</p> <ul> <li>Detect <strong>“first mismatch”</strong> as a special event.</li> <li>Consider a <strong>small number of controlled branches</strong> (here: two substrings).</li> <li>Keep overall complexity linear despite branching.</li> </ul> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="TwoPointers"/><category term="String"/><category term="Greedy"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Valid Palindrome II]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0345</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0345/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0345"/><published>2026-05-06T14:50:00+00:00</published><updated>2026-05-06T14:50:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0345</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0345/"><![CDATA[<h2 id="problem-0345-reverse-vowels-of-a-string">Problem 0345: Reverse Vowels of a String</h2> <p>When solving string problems on LeetCode, you’ll notice a recurring pattern:<br/> <strong>two pointers</strong> moving towards each other from the ends of the string.</p> <p>LeetCode 345 – <strong>Reverse Vowels of a String</strong> – is a beautiful, focused exercise on this pattern.<br/> It’s simpler than many “two-pointer” problems, yet rich enough to train your intuition for more advanced challenges.</p> <p>In this post, we’ll break down the problem, discuss naive and optimal ideas, and then walk step-by-step through an elegant two-pointer solution—aligned with the exact logic you implemented.</p> <hr/> <h2 id="1-problem-statement">1. Problem Statement</h2> <p>You are given a string <code class="language-plaintext highlighter-rouge">s</code>.<br/> Your task is to <strong>reverse only the vowels</strong> in the string and return the resulting string.</p> <ul> <li>Vowels are: <code class="language-plaintext highlighter-rouge">a, e, i, o, u, A, E, I, O, U</code></li> <li>Consonants, digits, and symbols must <strong>stay in their original positions</strong>.</li> <li>You only change the positions of vowels, by reversing their order.</li> </ul> <h4 id="11-example-1">1.1. Example 1</h4> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "hello"
Output: "holle"

Vowels: 'e' and 'o'
Reversing them: "holle"
</code></pre></div></div> <h4 id="12-example-2">1.2. Example 2</h4> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "leetcode"
Output: "leotcede"

Vowels in order: e, e, o, e
Reversed vowels: e, o, e, e
Final string: "leotcede"
</code></pre></div></div> <h4 id="example-3">Example 3</h4> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "IceCreAm"
Output: "AceCreIm"

Vowels (in order): I, e, e, A
Reversed: A, e, e, I
Consonants remain in place; only vowels are reordered.
</code></pre></div></div> <hr/> <h2 id="2-first-thoughts--a-straightforward-but-less-elegant-approach">2. First Thoughts – A Straightforward (But Less Elegant) Approach</h2> <p>The most direct idea might look like this:</p> <ol> <li>Traverse the string and <strong>collect all vowels</strong> in a list.</li> <li><strong>Reverse</strong> that list of vowels.</li> <li>Traverse the string again: <ul> <li>Every time you see a vowel, <strong>replace it</strong> with the next vowel from the reversed list.</li> </ul> </li> </ol> <p>This works.<br/> But what are the costs?</p> <ul> <li>You make <strong>multiple passes</strong> over the string.</li> <li>You maintain a list of all vowels.</li> <li>There’s extra management for indices into that reversed list.</li> </ul> <p>Time complexity is still <strong>O(n)</strong> and space complexity is also <strong>O(n)</strong>, but the solution feels less direct and less “in-place”.</p> <p>This is fine as a first attempt. However, there’s a cleaner and more “interview-friendly” pattern we can apply.</p> <hr/> <h2 id="3-the-two-pointer-idea">3. The Two-Pointer Idea</h2> <p>Two-pointer problems on strings or arrays usually come in one of these flavors:</p> <ul> <li><strong>Same direction:</strong> both pointers move forward (e.g., slow &amp; fast pointer).</li> <li><strong>Opposite directions:</strong> one pointer from the start, one from the end.</li> </ul> <p>This problem is clearly in the second category.</p> <p>Here’s the high-level idea:</p> <ol> <li>Convert the string to a <strong>mutable list of characters</strong> (since Python strings are immutable).</li> <li>Maintain two indices: <ul> <li><code class="language-plaintext highlighter-rouge">left</code> starting at the beginning (<code class="language-plaintext highlighter-rouge">0</code>)</li> <li><code class="language-plaintext highlighter-rouge">right</code> starting at the end (<code class="language-plaintext highlighter-rouge">len(s) - 1</code>)</li> </ul> </li> <li>While <code class="language-plaintext highlighter-rouge">left &lt; right</code>: <ul> <li>Move <code class="language-plaintext highlighter-rouge">left</code> to the <strong>right</strong> until it lands on a vowel.</li> <li>Move <code class="language-plaintext highlighter-rouge">right</code> to the <strong>left</strong> until it lands on a vowel.</li> <li>If both <code class="language-plaintext highlighter-rouge">s[left]</code> and <code class="language-plaintext highlighter-rouge">s[right]</code> are vowels: <ul> <li><strong>Swap</strong> them.</li> <li>Move both pointers inward.</li> </ul> </li> </ul> </li> </ol> <p>By doing this, we effectively <strong>reverse only the vowels</strong> while leaving all other characters untouched.</p> <p>This gives us:</p> <ul> <li>Single pass with two pointers → <strong>O(n)</strong> time.</li> <li>Only a list conversion overhead → <strong>O(n)</strong> space (standard for in-place swaps in Python).</li> </ul> <hr/> <h2 id="4-implementation--two-pointers-in-place-on-a-list">4. Implementation – Two Pointers, In-Place on a List</h2> <p>Let’s look at the core logic in Python, matching the algorithm you implemented.</p> <blockquote> <p>Note: For LeetCode’s expected signature, you’d add <code class="language-plaintext highlighter-rouge">self</code> as the first parameter, but here we focus on the core idea.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">reverseVowels</span><span class="p">(</span><span class="n">s</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
        
        <span class="n">vowels</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="sh">"</span><span class="s">aeiouAEIOU</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">s</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">s</span><span class="p">)</span>

        <span class="n">left</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span>

        <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>

            <span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">vowels</span><span class="p">:</span>
                <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
                <span class="k">continue</span>

            <span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">vowels</span><span class="p">:</span>
                <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
                <span class="k">continue</span>

            <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]</span> <span class="o">=</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span>

            <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>

        <span class="k">return</span> <span class="sh">""</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">s</span><span class="p">)</span>
</code></pre></div></div> <p>We’ll now dissect this line by line.</p> <hr/> <h2 id="5-detailed-walkthrough">5. Detailed Walkthrough</h2> <h3 id="51-defining-the-vowel-set-and-converting-string-to-list">5.1. Defining the Vowel Set and Converting String to List</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vowels</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="sh">"</span><span class="s">aeiouAEIOU</span><span class="sh">"</span><span class="p">)</span>
<span class="n">s</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">s</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">vowels</code> is a <code class="language-plaintext highlighter-rouge">set</code> of characters for <strong>O(1)</strong> lookup.</li> <li>Both lowercase and uppercase vowels are covered.</li> <li><code class="language-plaintext highlighter-rouge">s = list(s)</code> converts the string to a list of characters so we can <strong>swap in-place</strong>.</li> </ul> <h3 id="52-initializing-the-two-pointers">5.2. Initializing the Two Pointers</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">left</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">left</code> starts from the <strong>beginning</strong>.</li> <li><code class="language-plaintext highlighter-rouge">right</code> starts from the <strong>end</strong>.</li> </ul> <p>We will process the string from both ends towards the middle.</p> <h3 id="53-main-loop">5.3. Main Loop</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>
</code></pre></div></div> <p>We continue as long as <code class="language-plaintext highlighter-rouge">left</code> is strictly less than <code class="language-plaintext highlighter-rouge">right</code>.<br/> When <code class="language-plaintext highlighter-rouge">left &gt;= right</code>, all needed swaps are done.</p> <p>Inside this loop, we have three key components.</p> <hr/> <h3 id="531-move-left-until-it-reaches-a-vowel">5.3.1. Move <code class="language-plaintext highlighter-rouge">left</code> Until It Reaches a Vowel</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">vowels</span><span class="p">:</span>
    <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="k">continue</span>
</code></pre></div></div> <ul> <li>If <code class="language-plaintext highlighter-rouge">s[left]</code> is <strong>not</strong> a vowel: <ul> <li>Increment <code class="language-plaintext highlighter-rouge">left</code> and skip the rest of this iteration (<code class="language-plaintext highlighter-rouge">continue</code>).</li> </ul> </li> <li>This effectively means: <ul> <li>“Keep moving <code class="language-plaintext highlighter-rouge">left</code> forward until you find a vowel.”</li> </ul> </li> </ul> <hr/> <h3 id="532-move-right-until-it-reaches-a-vowel">5.3.2. Move <code class="language-plaintext highlighter-rouge">right</code> Until It Reaches a Vowel</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">vowels</span><span class="p">:</span>
    <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
    <span class="k">continue</span>
</code></pre></div></div> <ul> <li>If <code class="language-plaintext highlighter-rouge">s[right]</code> is <strong>not</strong> a vowel: <ul> <li>Decrement <code class="language-plaintext highlighter-rouge">right</code> and skip the rest of this iteration.</li> </ul> </li> <li>Interpretation: <ul> <li>“Keep moving <code class="language-plaintext highlighter-rouge">right</code> backward until you find a vowel.”</li> </ul> </li> </ul> <hr/> <h3 id="533-swap-the-two-vowels">5.3.3. Swap the Two Vowels</h3> <p>If we reach this part, it means:</p> <ul> <li><code class="language-plaintext highlighter-rouge">s[left]</code> is a vowel.</li> <li><code class="language-plaintext highlighter-rouge">s[right]</code> is a vowel.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]</span> <span class="o">=</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span>

<span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
<span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
</code></pre></div></div> <p>We:</p> <ol> <li>Swap the two vowels.</li> <li>Move <code class="language-plaintext highlighter-rouge">left</code> forward and <code class="language-plaintext highlighter-rouge">right</code> backward for the next pair.</li> </ol> <p>After enough iterations, we have reversed the order of vowels in the string.</p> <hr/> <h2 id="6-example-walkthrough-icecream">6. Example Walkthrough: <code class="language-plaintext highlighter-rouge">"IceCreAm"</code></h2> <p>Let’s trace your implementation on the example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  "IceCreAm"
</code></pre></div></div> <p>Initial setup:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">I</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">c</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">e</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">C</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">r</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">e</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">A</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">m</span><span class="sh">'</span><span class="p">]</span>
<span class="n">left</span> <span class="o">=</span> <span class="mi">0</span>
<span class="n">right</span> <span class="o">=</span> <span class="mi">7</span>
</code></pre></div></div> <h3 id="61-iteration-1">6.1. Iteration 1</h3> <ul> <li><code class="language-plaintext highlighter-rouge">s[left] = 'I'</code> → vowel</li> <li><code class="language-plaintext highlighter-rouge">s[right] = 'm'</code> → not vowel → <code class="language-plaintext highlighter-rouge">right -= 1</code> → <code class="language-plaintext highlighter-rouge">right = 6</code></li> </ul> <h3 id="62-iteration-2">6.2. Iteration 2</h3> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = 6</code></li> <li><code class="language-plaintext highlighter-rouge">s[left] = 'I'</code> → vowel</li> <li><code class="language-plaintext highlighter-rouge">s[right] = 'A'</code> → vowel</li> <li>Swap:</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="mi">6</span><span class="p">]</span> <span class="o">=</span> <span class="n">s</span><span class="p">[</span><span class="mi">6</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>  <span class="c1"># 'I' &lt;-&gt; 'A'
</span></code></pre></div></div> <p>Now:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">A</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">c</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">e</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">C</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">r</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">e</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">I</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">m</span><span class="sh">'</span><span class="p">]</span>
<span class="n">left</span> <span class="o">=</span> <span class="mi">1</span>
<span class="n">right</span> <span class="o">=</span> <span class="mi">5</span>
</code></pre></div></div> <h3 id="63-iteration-3">6.3. Iteration 3</h3> <ul> <li><code class="language-plaintext highlighter-rouge">s[1] = 'c'</code> → not vowel → <code class="language-plaintext highlighter-rouge">left = 2</code></li> <li><code class="language-plaintext highlighter-rouge">s[2] = 'e'</code> → vowel</li> <li><code class="language-plaintext highlighter-rouge">s[5] = 'e'</code> → vowel</li> <li>Swap <code class="language-plaintext highlighter-rouge">s[2]</code> and <code class="language-plaintext highlighter-rouge">s[5]</code>: <ul> <li>Both are ‘e’, so string stays the same.</li> </ul> </li> <li>Move pointers:</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">left</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">right</span> <span class="o">=</span> <span class="mi">4</span>
</code></pre></div></div> <h3 id="64-iteration-4">6.4. Iteration 4</h3> <ul> <li><code class="language-plaintext highlighter-rouge">s[3] = 'C'</code> → not vowel → <code class="language-plaintext highlighter-rouge">left = 4</code></li> <li>Now <code class="language-plaintext highlighter-rouge">left = 4</code>, <code class="language-plaintext highlighter-rouge">right = 4</code> → loop condition <code class="language-plaintext highlighter-rouge">left &lt; right</code> fails.</li> </ul> <p>End of loop.</p> <p>Final <code class="language-plaintext highlighter-rouge">s</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[</span><span class="sh">'</span><span class="s">A</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">c</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">e</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">C</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">r</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">e</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">I</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">m</span><span class="sh">'</span><span class="p">]</span>
</code></pre></div></div> <p>Return:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="sh">"</span><span class="s">AceCreIm</span><span class="sh">"</span>
</code></pre></div></div> <p>All vowels have been reversed while consonants stayed exactly where they were.</p> <hr/> <h2 id="7-time-and-space-complexity">7. Time and Space Complexity</h2> <h3 id="71-time-complexity">7.1. Time Complexity</h3> <ul> <li>Each pointer (<code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code>) moves at most <code class="language-plaintext highlighter-rouge">n</code> steps.</li> <li>In each iteration, we do constant work: <ul> <li>Comparisons,</li> <li>Pointer increments/decrements,</li> <li>Occasional swap.</li> </ul> </li> </ul> <p>Therefore:</p> <p>$\text{Time Complexity} = O(n)$</p> <p>where ( n = \text{len}(s) ).</p> <h3 id="72-space-complexity">7.2. Space Complexity</h3> <ul> <li>Converting the string to a list uses <strong>O(n)</strong> extra space.</li> <li>The <code class="language-plaintext highlighter-rouge">vowels</code> set is constant-sized: O(1).</li> </ul> <p>So:</p> <p>$\text{Space Complexity} = O(n)$</p> <p>In Python, this is a very standard and acceptable approach for in-place modification of strings.</p> <hr/> <h2 id="8-edge-cases-and-how-this-approach-handles-them">8. Edge Cases and How This Approach Handles Them</h2> <p>Let’s check some edge cases.</p> <h3 id="81-no-vowels">8.1. No Vowels</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="sh">"</span><span class="s">bcdfg</span><span class="sh">"</span>
</code></pre></div></div> <ul> <li>Both pointers move inward, but never find vowels to swap.</li> <li>The string remains unchanged.</li> <li>Our code behaves correctly.</li> </ul> <h3 id="82-all-vowels">8.2. All Vowels</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="sh">"</span><span class="s">aeiou</span><span class="sh">"</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">left</code> at ‘a’, <code class="language-plaintext highlighter-rouge">right</code> at ‘u’ → swap → ‘u’, ‘e’, ‘i’, ‘o’, ‘a’</li> <li>Then <code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code> move inward and swap ‘e’ and ‘o’.</li> <li>Final result: <code class="language-plaintext highlighter-rouge">"uoiea"</code> → vowels fully reversed.</li> </ul> <h3 id="83-single-character">8.3. Single Character</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="sh">"</span><span class="s">a</span><span class="sh">"</span> <span class="ow">or</span> <span class="n">s</span> <span class="o">=</span> <span class="sh">"</span><span class="s">b</span><span class="sh">"</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = 0</code></li> <li><code class="language-plaintext highlighter-rouge">left &lt; right</code> is false from the start.</li> <li>We immediately return the string.</li> </ul> <h3 id="84-empty-string">8.4. Empty String</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="sh">""</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = -1</code></li> <li>Loop never executes.</li> <li>Returns the empty string.</li> </ul> <h3 id="85-mix-of-uppercase-and-lowercase">8.5. Mix of Uppercase and Lowercase</h3> <p>Because our vowel set includes both lowercase and uppercase (<code class="language-plaintext highlighter-rouge">aeiouAEIOU</code>), cases like:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="sh">"</span><span class="s">AeIoU</span><span class="sh">"</span>
</code></pre></div></div> <p>are also correctly handled.</p> <hr/> <h2 id="9-pattern-recognition--why-this-problem-matters">9. Pattern Recognition – Why This Problem Matters</h2> <p>This problem is more than just vowel reversal. It trains a reusable mental template:</p> <blockquote> <p><strong>Two pointers moving from both ends, skipping irrelevant characters, and acting only on a specific subset.</strong></p> </blockquote> <p>You’ve already seen very similar ideas in:</p> <ul> <li><strong>LeetCode 125 – Valid Palindrome</strong> <ul> <li>Two pointers, skip non-alphanumeric characters, compare remaining ones.</li> </ul> </li> <li><strong>LeetCode 344 – Reverse String</strong> <ul> <li>Two pointers, swap characters directly from both ends.</li> </ul> </li> <li><strong>LeetCode 345 – Reverse Vowels of a String</strong> <ul> <li>Two pointers, but operate only when both sides land on vowels.</li> </ul> </li> </ul> <p>The core insight:</p> <ul> <li><strong>Filter &amp; act</strong> pattern: <ul> <li>Skip non-target characters.</li> <li>Apply logic when both sides are “interesting” (e.g., vowel, alphanumeric).</li> </ul> </li> <li>This generalizes to many string-processing questions.</li> </ul> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="TwoPointers"/><category term="String"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Reverse Vowels of a String]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0125</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0125/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0125"/><published>2026-05-06T14:10:00+00:00</published><updated>2026-05-06T14:10:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0125</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0125/"><![CDATA[<h2 id="problem-0125-valid-palindrome">Problem 0125: Valid Palindrome</h2> <p>In the world of string problems on LeetCode, <strong>Valid Palindrome (Problem 125)</strong> is one of those questions that looks trivial at first glance but hides a couple of important <strong>interview patterns</strong> inside:</p> <ul> <li><strong>Two-pointer technique</strong> (from both ends),</li> <li><strong>Character filtering</strong> (only alphanumeric),</li> <li><strong>Case-insensitive comparison</strong>.</li> </ul> <p>If you master this problem properly, it will pay off later in more advanced questions like:</p> <ul> <li><strong>680 – Valid Palindrome II</strong> (allowing at most one deletion),</li> <li>Palindrome-based substring problems,</li> <li>Complex parsing + validation tasks.</li> </ul> <p>In this post, we’ll go through:</p> <ol> <li>Problem overview</li> <li>Why this is more than “just check if it’s a palindrome”</li> <li>The two-pointer + filtering intuition</li> <li>Step-by-step Python implementation</li> <li>Detailed walkthrough on examples</li> <li>Time &amp; space complexity</li> <li>Common mistakes in interviews</li> <li>How this pattern generalizes to harder problems</li> </ol> <hr/> <h2 id="1-problem-overview">1. Problem Overview</h2> <p><strong>LeetCode 125 – Valid Palindrome</strong></p> <blockquote> <p>Given a string <code class="language-plaintext highlighter-rouge">s</code>, determine if it is a palindrome, considering only <strong>alphanumeric characters</strong> and <strong>ignoring cases</strong>.</p> </blockquote> <ul> <li>Letters (<code class="language-plaintext highlighter-rouge">a–z</code>, <code class="language-plaintext highlighter-rouge">A–Z</code>) and digits (<code class="language-plaintext highlighter-rouge">0–9</code>) count.</li> <li>Everything else (spaces, commas, colons, punctuation, etc.) should be <strong>ignored</strong>.</li> <li>Uppercase and lowercase should be treated as the <strong>same</strong>.</li> </ul> <h3 id="11-example-1">1.1. Example 1</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "A man, a plan, a canal: Panama"
Output: true
Explanation:
  After removing non-alphanumeric characters and lowercasing,
  we get "amanaplanacanalpanama", which is a palindrome.
</code></pre></div></div> <h3 id="12-example-2">1.2. Example 2</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = "race a car"
Output: false
Explanation:
  After filtering, "raceacar" is not a palindrome.
</code></pre></div></div> <hr/> <h2 id="2-why-this-problem-is-subtle">2. Why This Problem Is Subtle</h2> <p>At first, you might think:</p> <blockquote> <p>“Easy, I’ll just reverse the string and compare.”</p> </blockquote> <p>But there are three important constraints:</p> <ol> <li><strong>Only alphanumeric characters</strong> <ul> <li>You must ignore spaces, commas, punctuation, etc.</li> </ul> </li> <li><strong>Case-insensitive</strong> <ul> <li><code class="language-plaintext highlighter-rouge">A</code> and <code class="language-plaintext highlighter-rouge">a</code> are equal.</li> </ul> </li> <li><strong>Efficiency &amp; cleanliness</strong> <ul> <li>Interviewers don’t want a messy solution with lots of ad-hoc filtering logic.</li> <li>They want to see either: <ul> <li>A clean <strong>filtered string + reverse check</strong>, or</li> <li>A more optimal and elegant <strong>two-pointer</strong> solution.</li> </ul> </li> </ul> </li> </ol> <p>This is what makes it a good filter question in interviews:<br/> They want to see if you can <strong>apply a standard pattern</strong> (two-pointer) to slightly noisy input (non-alphanumeric characters, case differences).</p> <hr/> <h2 id="3-intuition-two-pointers--filtering">3. Intuition: Two Pointers + Filtering</h2> <p>The core idea:</p> <ul> <li>A <strong>palindrome</strong> reads the same forward and backward.</li> <li>Instead of building a new filtered string and reversing it, we can check directly on the original string using two pointers:</li> </ul> <h3 id="31-high-level-strategy">3.1. High-level strategy</h3> <ol> <li>Convert the entire string to <strong>lowercase</strong> (to ignore case).</li> <li>Use two indices: <ul> <li><code class="language-plaintext highlighter-rouge">left</code> starting at the <strong>beginning</strong>,</li> <li><code class="language-plaintext highlighter-rouge">right</code> starting at the <strong>end</strong>.</li> </ul> </li> <li>While <code class="language-plaintext highlighter-rouge">left &lt; right</code>: <ul> <li>If <code class="language-plaintext highlighter-rouge">s[left]</code> is <strong>not alphanumeric</strong>, move <code class="language-plaintext highlighter-rouge">left</code> forward.</li> <li>If <code class="language-plaintext highlighter-rouge">s[right]</code> is <strong>not alphanumeric</strong>, move <code class="language-plaintext highlighter-rouge">right</code> backward.</li> <li>Once both point to alphanumeric characters, <strong>compare</strong>: <ul> <li>If <code class="language-plaintext highlighter-rouge">s[left] != s[right]</code> → not a palindrome.</li> <li>If they’re equal → move both pointers inward and continue.</li> </ul> </li> </ul> </li> <li>If the pointers cross without a mismatch → it <strong>is</strong> a palindrome.</li> </ol> <p>This pattern is extremely powerful:</p> <ul> <li>You don’t create extra strings (O(1) extra memory).</li> <li>You traverse each character at most once (O(n) time).</li> <li>Your code ends up clean and readable.</li> </ul> <hr/> <h2 id="4-python-implementation-leetcode-ready">4. Python Implementation (LeetCode-Ready)</h2> <p>Here is a clean implementation, using exactly this two-pointer + filtering idea:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">isPalindrome</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">s</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">bool</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Returns True if s is a valid palindrome, considering only alphanumeric
        characters and ignoring case. Otherwise returns False.
        </span><span class="sh">"""</span>

        <span class="c1"># Normalize to lowercase for case-insensitive comparison
</span>        <span class="n">s</span> <span class="o">=</span> <span class="n">s</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span>
        <span class="n">left</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span>

        <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>
            <span class="c1"># Move left pointer to the next alphanumeric character
</span>            <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span> <span class="ow">and</span> <span class="ow">not</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">].</span><span class="nf">isalnum</span><span class="p">():</span>
                <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>

            <span class="c1"># Move right pointer to the previous alphanumeric character
</span>            <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span> <span class="ow">and</span> <span class="ow">not</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">].</span><span class="nf">isalnum</span><span class="p">():</span>
                <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>

            <span class="c1"># Now both s[left] and s[right] are alphanumeric
</span>            <span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">!=</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]:</span>
                <span class="k">return</span> <span class="bp">False</span>

            <span class="c1"># Move both pointers towards the center
</span>            <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>

        <span class="c1"># No mismatches found
</span>        <span class="k">return</span> <span class="bp">True</span>
</code></pre></div></div> <h3 id="41-why-this-is-clean-and-robust">4.1. Why this is clean and robust</h3> <ul> <li><code class="language-plaintext highlighter-rouge">s.lower()</code> handles case-insensitivity in one line.</li> <li><code class="language-plaintext highlighter-rouge">str.isalnum()</code> handles the “only alphanumeric” condition cleanly.</li> <li>We avoid building a new filtered string; we use the original string directly.</li> <li>The logic is fully <strong>single-pass</strong> and <strong>in-place</strong> in terms of memory.</li> </ul> <hr/> <h2 id="5-step-by-step-walkthrough">5. Step-by-Step Walkthrough</h2> <p>Let’s walk through the algorithm on a few representative inputs.</p> <h3 id="51-example-1-a-man-a-plan-a-canal-panama">5.1. Example 1: <code class="language-plaintext highlighter-rouge">"A man, a plan, a canal: Panama"</code></h3> <ol> <li>Lowercase: <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"a man, a plan, a canal: panama"
</code></pre></div> </div> </li> <li>Initial pointers: <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code> → <code class="language-plaintext highlighter-rouge">'a'</code></li> <li><code class="language-plaintext highlighter-rouge">right = len(s) - 1</code> → <code class="language-plaintext highlighter-rouge">'a'</code> (last character)</li> </ul> </li> </ol> <p><strong>Iteration 1:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">s[left] = 'a'</code> (alnum), <code class="language-plaintext highlighter-rouge">s[right] = 'a'</code> (alnum)</li> <li>Compare: <code class="language-plaintext highlighter-rouge">'a' == 'a'</code> → OK</li> <li>Move: <ul> <li><code class="language-plaintext highlighter-rouge">left = 1</code></li> <li><code class="language-plaintext highlighter-rouge">right = right - 1</code></li> </ul> </li> </ul> <p><strong>Iteration 2:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">s[left] = ' '</code> (space, not alnum) <ul> <li>Inner loop moves <code class="language-plaintext highlighter-rouge">left</code> forward until it hits <code class="language-plaintext highlighter-rouge">'m'</code>.</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">s[right]</code> might be <code class="language-plaintext highlighter-rouge">'m'</code> or another letter depending on string length, but it will be alnum.</li> </ul> <p>Compare:</p> <ul> <li><code class="language-plaintext highlighter-rouge">'m' == 'm'</code> → OK</li> <li>Move pointers inward.</li> </ul> <p>This continues:</p> <ul> <li>Spaces, commas, and the colon are skipped.</li> <li>Only letters/digits are compared.</li> <li>Every pair matches.</li> <li>Eventually <code class="language-plaintext highlighter-rouge">left &gt;= right</code> and we never saw a mismatch → return <code class="language-plaintext highlighter-rouge">True</code>.</li> </ul> <hr/> <h3 id="52-example-2-race-a-car">5.2. Example 2: <code class="language-plaintext highlighter-rouge">"race a car"</code></h3> <p>Conceptual filtered + lowercased version is <code class="language-plaintext highlighter-rouge">"raceacar"</code>.</p> <p>Walking through:</p> <ul> <li><code class="language-plaintext highlighter-rouge">r</code> vs <code class="language-plaintext highlighter-rouge">r</code> → OK</li> <li><code class="language-plaintext highlighter-rouge">a</code> vs <code class="language-plaintext highlighter-rouge">a</code> → OK</li> <li><code class="language-plaintext highlighter-rouge">c</code> vs <code class="language-plaintext highlighter-rouge">c</code> → OK</li> <li><code class="language-plaintext highlighter-rouge">e</code> vs <code class="language-plaintext highlighter-rouge">a</code> → mismatch → return <code class="language-plaintext highlighter-rouge">False</code>.</li> </ul> <p>The key: the mismatch detection happens as soon as character pairs disagree.</p> <hr/> <h3 id="53-example-3--empty-string">5.3. Example 3: <code class="language-plaintext highlighter-rouge">""</code> (empty string)</h3> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = -1</code>.</li> <li><code class="language-plaintext highlighter-rouge">while left &lt; right</code> is <code class="language-plaintext highlighter-rouge">False</code> immediately.</li> <li>We never enter the loop → we return <code class="language-plaintext highlighter-rouge">True</code>.</li> </ul> <p>By convention, an empty string is considered a palindrome.</p> <hr/> <h3 id="54-example-4-">5.4. Example 4: <code class="language-plaintext highlighter-rouge">".,,,!!!"</code></h3> <ul> <li>All characters are non-alphanumeric.</li> <li><code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code> keep moving inward until <code class="language-plaintext highlighter-rouge">left &gt;= right</code>.</li> <li>No mismatches are found → returns <code class="language-plaintext highlighter-rouge">True</code>.</li> </ul> <p>This is a subtle edge case:<br/> With “no letters/digits at all”, we treat it as trivially symmetric.</p> <hr/> <h2 id="6-time-and-space-complexity">6. Time and Space Complexity</h2> <p>Let <code class="language-plaintext highlighter-rouge">n</code> be the length of the string <code class="language-plaintext highlighter-rouge">s</code>.</p> <h3 id="61-time-complexity-on">6.1. Time Complexity: O(n)</h3> <ul> <li>Each pointer (<code class="language-plaintext highlighter-rouge">left</code>, <code class="language-plaintext highlighter-rouge">right</code>) moves <strong>at most n steps</strong>.</li> <li>Inner loops skip non-alphanumeric characters, but each character is visited at most once by each pointer.</li> <li>There are no nested loops over the entire string.</li> </ul> <p>Formally:</p> <p>$\text{Time Complexity} = O(n)$</p> <h3 id="62-space-complexity-o1">6.2. Space Complexity: O(1)</h3> <ul> <li>We use only a handful of variables (<code class="language-plaintext highlighter-rouge">left</code>, <code class="language-plaintext highlighter-rouge">right</code>, modified <code class="language-plaintext highlighter-rouge">s</code> reference).</li> <li>No auxiliary data structures like extra lists or arrays (beyond the input string).</li> </ul> <p>$\text{Space Complexity} = O(1)$</p> <p>(If you consider <code class="language-plaintext highlighter-rouge">s.lower()</code> creating a new string, that’s usually seen as O(n) transformation, but in interview context, focus is on <em>extra</em> data structures; here we don’t store more than a constant number of variables beyond that.)</p> <hr/> <h2 id="7-common-mistakes-in-interviews">7. Common Mistakes in Interviews</h2> <p>This problem appears simple, but candidates frequently stumble on subtle constraints. Here are typical pitfalls:</p> <h3 id="71-mistake-1-forgetting-to-ignore-non-alphanumeric-characters">7.1. Mistake 1: Forgetting to Ignore Non-Alphanumeric Characters</h3> <p>Many candidates write something like:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="n">s</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span>
<span class="k">return</span> <span class="n">s</span> <span class="o">==</span> <span class="n">s</span><span class="p">[::</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
</code></pre></div></div> <p>This fails on:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="sh">"</span><span class="s">A man, a plan, a canal: Panama</span><span class="sh">"</span>
</code></pre></div></div> <p>Because:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"aman, a plan, a canal: panama" != "amanap lanalac :lanap ,nam a"
</code></pre></div></div> <p>They ignore spaces and punctuation requirements.</p> <p><strong>Fix:</strong> Either filter the string first or skip non-alnum characters in the loop.</p> <hr/> <h3 id="72-mistake-2-not-handling-case-insensitivity">7.2. Mistake 2: Not Handling Case-Insensitivity</h3> <p>Comparing directly without handling case:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">!=</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]:</span>
    <span class="k">return</span> <span class="bp">False</span>
</code></pre></div></div> <p>Without lowercasing or uppercasing leads to incorrect behavior for inputs like <code class="language-plaintext highlighter-rouge">"Aa"</code> or <code class="language-plaintext highlighter-rouge">"A man..."</code>.</p> <p><strong>Fix:</strong> Always normalize:</p> <ul> <li>Either <code class="language-plaintext highlighter-rouge">s = s.lower()</code> at the start,</li> <li>Or compare with <code class="language-plaintext highlighter-rouge">s[left].lower() == s[right].lower()</code>.</li> </ul> <hr/> <h3 id="73-mistake-3-building-a-filtered-string-but-forgetting-digits">7.3. Mistake 3: Building a Filtered String but Forgetting Digits</h3> <p>Some candidates only allow letters and forget digits:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">filtered</span> <span class="o">=</span> <span class="p">[</span><span class="n">ch</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span> <span class="k">for</span> <span class="n">ch</span> <span class="ow">in</span> <span class="n">s</span> <span class="k">if</span> <span class="n">ch</span><span class="p">.</span><span class="nf">isalpha</span><span class="p">()]</span>
</code></pre></div></div> <p>This breaks for inputs with digits like <code class="language-plaintext highlighter-rouge">"0P"</code> or <code class="language-plaintext highlighter-rouge">"12321"</code>.</p> <p><strong>Correct:</strong> Use <code class="language-plaintext highlighter-rouge">isalnum()</code> instead of <code class="language-plaintext highlighter-rouge">isalpha()</code> to include digits.</p> <hr/> <h3 id="74-mistake-4-overcomplicating-the-solution">7.4. Mistake 4: Overcomplicating the Solution</h3> <p>Sometimes people try to:</p> <ul> <li>Use multiple extra strings,</li> <li>Use regex-based heavy filtering,</li> <li>Or write multiple passes over the string.</li> </ul> <p>While such solutions may work, they often:</p> <ul> <li>Increase code complexity,</li> <li>Make the algorithm harder to explain under interview time pressure.</li> </ul> <p>Interviewers prefer a <strong>clean, single-pass two-pointer</strong> solution.</p> <hr/> <h3 id="75-mistake-5-off-by-one-errors-with-pointers">7.5. Mistake 5: Off-by-One Errors with Pointers</h3> <p>Typical mistakes:</p> <ul> <li>Using <code class="language-plaintext highlighter-rouge">left &lt;= right</code> and mishandling the middle character,</li> <li>Forgetting to check <code class="language-plaintext highlighter-rouge">left &lt; right</code> in the inner loops before accessing <code class="language-plaintext highlighter-rouge">s[left]</code> or <code class="language-plaintext highlighter-rouge">s[right]</code>.</li> </ul> <p>Correct pattern:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>
    <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span> <span class="ow">and</span> <span class="ow">not</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">].</span><span class="nf">isalnum</span><span class="p">():</span>
        <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span> <span class="ow">and</span> <span class="ow">not</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">].</span><span class="nf">isalnum</span><span class="p">():</span>
        <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
    <span class="c1"># Compare...
</span></code></pre></div></div> <p>Notice how <code class="language-plaintext highlighter-rouge">left &lt; right</code> is present in the inner loops too, to avoid indexing issues.</p> <hr/> <h2 id="8-from-valid-palindrome-to-advanced-patterns">8. From Valid Palindrome to Advanced Patterns</h2> <p>Once you’re comfortable with this pattern, you can naturally extend it to more complex problems.</p> <h3 id="81-leetcode-680--valid-palindrome-ii">8.1. LeetCode 680 – Valid Palindrome II</h3> <ul> <li>Problem: Check if a string can become a palindrome by deleting at most <strong>one character</strong>.</li> <li>Core insight: <ul> <li>Same two-pointer setup.</li> <li>On first mismatch, try: <ul> <li>Skipping <code class="language-plaintext highlighter-rouge">left</code> once.</li> <li>Or skipping <code class="language-plaintext highlighter-rouge">right</code> once.</li> </ul> </li> <li>Check if either resulting substring is a palindrome.</li> </ul> </li> </ul> <p>Your current solution for 125 essentially becomes a <strong>subroutine</strong> for 680.</p> <hr/> <h3 id="82-palindromic-substrings--longest-palindromic-substring">8.2. Palindromic Substrings / Longest Palindromic Substring</h3> <p>There you usually use a different pattern:</p> <ul> <li><strong>Expand around center</strong>,</li> <li>Or dynamic programming,</li> <li>But the mental model of “mirror symmetry” from Valid Palindrome still helps.</li> </ul> <hr/> <h3 id="83-general-data-cleaning--validation-problems">8.3. General Data Cleaning + Validation Problems</h3> <p>The combination of:</p> <ul> <li>Input normalization (lowercase, trimming),</li> <li>Filtering irrelevant characters (<code class="language-plaintext highlighter-rouge">isalnum()</code>),</li> <li>Two-pointer or linear scan,</li> </ul> <p>appears in:</p> <ul> <li>Log parsing,</li> <li>Input sanitization,</li> <li>Canonicalization tasks.</li> </ul> <p>This is more than a LeetCode trick – it’s a real-life pattern.</p> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="TwoPointers"/><category term="String"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Valid Palindrome]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0977</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0977/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0977"/><published>2026-05-05T14:50:00+00:00</published><updated>2026-05-05T14:50:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0977</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0977/"><![CDATA[<h2 id="problem-0977-squares-of-a-sorted-array">Problem 0977: Squares of a Sorted Array</h2> <p>At first glance, <strong>LeetCode 977 – Squares of a Sorted Array</strong> looks trivial:</p> <blockquote> <p>“Square all elements and sort the result.”</p> </blockquote> <p>But if you’re serious about algorithmic thinking (and not just passing the problem), this question is a perfect playground for:</p> <ul> <li>Understanding how to <strong>leverage existing sorting</strong>,</li> <li>Practicing the <strong>two-pointer pattern</strong>,</li> <li>Optimizing from <code class="language-plaintext highlighter-rouge">O(n log n)</code> down to <code class="language-plaintext highlighter-rouge">O(n)</code>.</li> </ul> <p>In this post, we’ll walk through:</p> <ul> <li>Problem statement (clearly and briefly),</li> <li>The naive approach and why it’s suboptimal,</li> <li>The key insight behind the optimal solution,</li> <li>A clean <strong>two-pointer</strong> implementation in Python,</li> <li>A detailed step-by-step walkthrough,</li> <li>Time &amp; space complexity,</li> <li>Common mistakes in interviews,</li> <li>And how this pattern connects to other LeetCode problems (like 88, 283, 344).</li> </ul> <hr/> <h2 id="1-problem-statement">1. Problem Statement</h2> <p>You are given a sorted integer array in <strong>non-decreasing order</strong>:</p> <ul> <li>It may contain negative numbers,</li> <li>Zero,</li> <li>And positive numbers.</li> </ul> <p>You must:</p> <ul> <li>Square each number,</li> <li>And return a <strong>new array</strong> of the squares,</li> <li>Also sorted in <strong>non-decreasing</strong> order.</li> </ul> <h3 id="11-example-1">1.1. Example 1</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  nums = [-4, -1, 0, 3, 10]
Output: [0, 1, 9, 16, 100]
</code></pre></div></div> <h3 id="11-example-2">1.1. Example 2</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  nums = [-7, -3, 2, 3, 11]
Output: [4, 9, 9, 49, 121]
</code></pre></div></div> <p>Constraints (simplified):</p> <ul> <li>The array is already sorted.</li> <li>Length is up to <code class="language-plaintext highlighter-rouge">10^4</code> (or larger depending on variant).</li> <li>Values can be negative.</li> </ul> <hr/> <h2 id="2-first-thought-square-then-sort">2. First Thought: Square Then Sort</h2> <p>The typical first idea:</p> <ol> <li>Iterate through <code class="language-plaintext highlighter-rouge">nums</code>,</li> <li>Replace each element with its square,</li> <li>Sort the array.</li> </ol> <p>Pseudo-code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">nums</span><span class="p">)):</span>
    <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">*</span> <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>

<span class="n">nums</span><span class="p">.</span><span class="nf">sort</span><span class="p">()</span>
<span class="k">return</span> <span class="n">nums</span>
</code></pre></div></div> <p>This solution:</p> <ul> <li>Is simple and works,</li> <li>Runs in: <ul> <li><code class="language-plaintext highlighter-rouge">O(n)</code> for squaring,</li> <li><code class="language-plaintext highlighter-rouge">O(n log n)</code> for sorting.</li> </ul> </li> </ul> <p>Total time complexity: <strong>O(n log n)</strong>.</p> <p>For many real-world scenarios, this is good enough. But you’re on LeetCode / in an interview – this is where the fun starts:</p> <blockquote> <p>The input array is <strong>already sorted</strong>. Ignoring that fact and sorting again is a waste of information.</p> </blockquote> <p>Can we do better and achieve <strong>O(n)</strong> time?</p> <p>Yes. And the key is to think about <strong>absolute values</strong> and <strong>two pointers</strong>.</p> <hr/> <h2 id="3-core-insight-largest-square-comes-from-the-ends">3. Core Insight: Largest Square Comes from the Ends</h2> <p>Remember:</p> <ul> <li><code class="language-plaintext highlighter-rouge">nums</code> is sorted in non-decreasing order: <ul> <li>Example: <code class="language-plaintext highlighter-rouge">[-4, -1, 0, 3, 10]</code>.</li> </ul> </li> <li>Squaring changes the order because: <ul> <li>Negative values become positive.</li> <li>Large negative numbers (e.g. <code class="language-plaintext highlighter-rouge">-4</code>) can have larger squares than small positive numbers (e.g. <code class="language-plaintext highlighter-rouge">3</code>).</li> </ul> </li> </ul> <p>Key observation:</p> <ul> <li>The largest square must come from either: <ul> <li>The <strong>leftmost</strong> element (<code class="language-plaintext highlighter-rouge">nums[0]</code>, most negative), or</li> <li>The <strong>rightmost</strong> element (<code class="language-plaintext highlighter-rouge">nums[n-1]</code>, most positive), because those are the elements with the largest absolute values.</li> </ul> </li> </ul> <p>So instead of:</p> <ul> <li>Squaring everything and sorting blindly,</li> </ul> <p>we can:</p> <ul> <li>Use two pointers (<code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code>),</li> <li>Compare <code class="language-plaintext highlighter-rouge">abs(nums[left])</code> and <code class="language-plaintext highlighter-rouge">abs(nums[right])</code>,</li> <li>Take the <strong>larger square</strong> and place it at the <strong>end</strong> of the result array.</li> </ul> <p>This leads directly to a <strong>two-pointer, O(n)</strong> solution.</p> <hr/> <h2 id="4-strategy-two-pointers--fill-from-the-back">4. Strategy: Two Pointers + Fill from the Back</h2> <p>We’ll build a result array <code class="language-plaintext highlighter-rouge">result</code> of the same size as <code class="language-plaintext highlighter-rouge">nums</code>:</p> <ul> <li>Let <code class="language-plaintext highlighter-rouge">left = 0</code>, start of <code class="language-plaintext highlighter-rouge">nums</code></li> <li>Let <code class="language-plaintext highlighter-rouge">right = n - 1</code>, end of <code class="language-plaintext highlighter-rouge">nums</code></li> <li>Let <code class="language-plaintext highlighter-rouge">k</code> go from <code class="language-plaintext highlighter-rouge">n - 1</code> down to <code class="language-plaintext highlighter-rouge">0</code> (fill <code class="language-plaintext highlighter-rouge">result</code> from right to left)</li> </ul> <p>At each step:</p> <ol> <li>Compute: <ul> <li><code class="language-plaintext highlighter-rouge">square_left = nums[left] * nums[left]</code></li> <li><code class="language-plaintext highlighter-rouge">square_right = nums[right] * nums[right]</code></li> </ul> </li> <li>Compare: <ul> <li>If <code class="language-plaintext highlighter-rouge">square_left &gt; square_right</code>: <ul> <li>Set <code class="language-plaintext highlighter-rouge">result[k] = square_left</code></li> <li>Move <code class="language-plaintext highlighter-rouge">left</code> one step right (<code class="language-plaintext highlighter-rouge">left += 1</code>)</li> </ul> </li> <li>Else: <ul> <li>Set <code class="language-plaintext highlighter-rouge">result[k] = square_right</code></li> <li>Move <code class="language-plaintext highlighter-rouge">right</code> one step left (<code class="language-plaintext highlighter-rouge">right -= 1</code>)</li> </ul> </li> </ul> </li> <li>Decrease <code class="language-plaintext highlighter-rouge">k</code> and repeat.</li> </ol> <p>When the loop ends, <code class="language-plaintext highlighter-rouge">result</code> is fully populated and <strong>sorted</strong>.</p> <hr/> <h2 id="5-python-implementation-two-pointer-on">5. Python Implementation (Two-Pointer, O(n))</h2> <p>Here is a clean implementation that matches exactly this logic (and aligns with LeetCode’s expectations):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">sortedSquares</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">nums</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">]:</span>
        <span class="sh">"""</span><span class="s">
        Given a sorted integer array nums, returns an array of the squares
        of each number, also sorted in non-decreasing order.

        This uses a two-pointer approach:
        - One pointer at the beginning (left)
        - One pointer at the end (right)
        - Fill the result array from the largest square (end) backwards

        Time Complexity:  O(n)
        Space Complexity: O(1) extra (excluding the output array)
        </span><span class="sh">"""</span>

        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span>
        <span class="n">result</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">n</span>
        <span class="n">right</span><span class="p">,</span> <span class="n">left</span> <span class="o">=</span> <span class="n">n</span> <span class="o">-</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">0</span>

        <span class="c1"># Fill result from the end (largest square) towards the beginning
</span>        <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n</span> <span class="o">-</span> <span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">):</span>
            <span class="n">square_left</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">*</span> <span class="n">nums</span><span class="p">[</span><span class="n">left</span><span class="p">]</span>
            <span class="n">square_right</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">right</span><span class="p">]</span> <span class="o">*</span> <span class="n">nums</span><span class="p">[</span><span class="n">right</span><span class="p">]</span>

            <span class="k">if</span> <span class="n">square_left</span> <span class="o">&gt;</span> <span class="n">square_right</span><span class="p">:</span>
                <span class="n">result</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="o">=</span> <span class="n">square_left</span>
                <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="n">result</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="o">=</span> <span class="n">square_right</span>
                <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>

        <span class="k">return</span> <span class="n">result</span>
</code></pre></div></div> <p>This is the same algorithmic idea you implemented: optimal, simple, and clean.</p> <hr/> <h2 id="6-dry-run-understanding-the-flow">6. Dry Run: Understanding the Flow</h2> <p>Let’s walk through the classic example step-by-step:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="o">-</span><span class="mi">4</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">10</span><span class="p">]</span>
</code></pre></div></div> <p>Initial:</p> <ul> <li><code class="language-plaintext highlighter-rouge">n = 5</code></li> <li><code class="language-plaintext highlighter-rouge">result = [0, 0, 0, 0, 0]</code></li> <li><code class="language-plaintext highlighter-rouge">left = 0</code> → <code class="language-plaintext highlighter-rouge">nums[left] = -4</code></li> <li><code class="language-plaintext highlighter-rouge">right = 4</code> → <code class="language-plaintext highlighter-rouge">nums[right] = 10</code></li> </ul> <p>We will fill <code class="language-plaintext highlighter-rouge">result</code> from index <code class="language-plaintext highlighter-rouge">k = 4</code> down to <code class="language-plaintext highlighter-rouge">0</code>.</p> <hr/> <h3 id="61-iteration-1-k--4">6.1. Iteration 1: k = 4</h3> <ul> <li><code class="language-plaintext highlighter-rouge">square_left = (-4)^2 = 16</code></li> <li><code class="language-plaintext highlighter-rouge">square_right = 10^2 = 100</code></li> <li><code class="language-plaintext highlighter-rouge">100 &gt; 16</code> → use <code class="language-plaintext highlighter-rouge">square_right</code></li> </ul> <p>So:</p> <ul> <li><code class="language-plaintext highlighter-rouge">result[4] = 100</code></li> <li>Move <code class="language-plaintext highlighter-rouge">right</code> left: <code class="language-plaintext highlighter-rouge">right = 3</code></li> </ul> <p>Current state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>result = [0, 0, 0, 0, 100]
left   = 0  (nums[left] = -4)
right  = 3  (nums[right] = 3)
</code></pre></div></div> <hr/> <h3 id="62-iteration-2-k--3">6.2. Iteration 2: k = 3</h3> <ul> <li><code class="language-plaintext highlighter-rouge">square_left = (-4)^2 = 16</code></li> <li><code class="language-plaintext highlighter-rouge">square_right = 3^2 = 9</code></li> <li><code class="language-plaintext highlighter-rouge">16 &gt; 9</code> → use <code class="language-plaintext highlighter-rouge">square_left</code></li> </ul> <p>So:</p> <ul> <li><code class="language-plaintext highlighter-rouge">result[3] = 16</code></li> <li>Move <code class="language-plaintext highlighter-rouge">left</code> right: <code class="language-plaintext highlighter-rouge">left = 1</code></li> </ul> <p>Current state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>result = [0, 0, 0, 16, 100]
left   = 1  (nums[left] = -1)
right  = 3  (nums[right] = 3)
</code></pre></div></div> <hr/> <h3 id="63-iteration-3-k--2">6.3. Iteration 3: k = 2</h3> <ul> <li><code class="language-plaintext highlighter-rouge">square_left = (-1)^2 = 1</code></li> <li><code class="language-plaintext highlighter-rouge">square_right = 3^2 = 9</code></li> <li><code class="language-plaintext highlighter-rouge">9 &gt; 1</code> → use <code class="language-plaintext highlighter-rouge">square_right</code></li> </ul> <p>So:</p> <ul> <li><code class="language-plaintext highlighter-rouge">result[2] = 9</code></li> <li>Move <code class="language-plaintext highlighter-rouge">right</code> left: <code class="language-plaintext highlighter-rouge">right = 2</code></li> </ul> <p>Current state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>result = [0, 0, 9, 16, 100]
left   = 1  (nums[left] = -1)
right  = 2  (nums[right] = 0)
</code></pre></div></div> <hr/> <h3 id="64-iteration-4-k--1">6.4. Iteration 4: k = 1</h3> <ul> <li><code class="language-plaintext highlighter-rouge">square_left = (-1)^2 = 1</code></li> <li><code class="language-plaintext highlighter-rouge">square_right = 0^2 = 0</code></li> <li><code class="language-plaintext highlighter-rouge">1 &gt; 0</code> → use <code class="language-plaintext highlighter-rouge">square_left</code></li> </ul> <p>So:</p> <ul> <li><code class="language-plaintext highlighter-rouge">result[1] = 1</code></li> <li>Move <code class="language-plaintext highlighter-rouge">left</code> right: <code class="language-plaintext highlighter-rouge">left = 2</code></li> </ul> <p>Current state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>result = [0, 1, 9, 16, 100]
left   = 2  (nums[left] = 0)
right  = 2  (nums[right] = 0)
</code></pre></div></div> <hr/> <h3 id="65-iteration-5-k--0">6.5. Iteration 5: k = 0</h3> <ul> <li><code class="language-plaintext highlighter-rouge">square_left = 0^2 = 0</code></li> <li><code class="language-plaintext highlighter-rouge">square_right = 0^2 = 0</code></li> <li>Equal → else-branch used (doesn’t matter which one we pick)</li> </ul> <p>So:</p> <ul> <li><code class="language-plaintext highlighter-rouge">result[0] = 0</code></li> <li>Move <code class="language-plaintext highlighter-rouge">right</code> left: <code class="language-plaintext highlighter-rouge">right = 1</code></li> </ul> <p>Final:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>result = [0, 1, 9, 16, 100]
</code></pre></div></div> <p>We obtained the correct sorted squares in a single pass.</p> <hr/> <h2 id="7-time-and-space-complexity">7. Time and Space Complexity</h2> <h3 id="71-time-complexity">7.1. Time Complexity</h3> <ul> <li>We have a single loop from <code class="language-plaintext highlighter-rouge">k = n-1</code> down to <code class="language-plaintext highlighter-rouge">0</code>.</li> <li>Each iteration does constant-time operations: a few multiplications, comparisons, and assignments.</li> </ul> <p>Therefore:</p> <p>$\text{Time Complexity} = O(n)$</p> <p>This improves on the naive <code class="language-plaintext highlighter-rouge">O(n \log n)</code> solution that squares then sorts.</p> <h3 id="72-space-complexity">7.2. Space Complexity</h3> <ul> <li>We allocate a <code class="language-plaintext highlighter-rouge">result</code> array of size <code class="language-plaintext highlighter-rouge">n</code> (required by the problem).</li> <li>Apart from that, we use only constant extra variables: <code class="language-plaintext highlighter-rouge">left</code>, <code class="language-plaintext highlighter-rouge">right</code>, <code class="language-plaintext highlighter-rouge">k</code>, <code class="language-plaintext highlighter-rouge">square_left</code>, <code class="language-plaintext highlighter-rouge">square_right</code>.</li> </ul> <p>So:</p> <p>$\text{Space Complexity} = O(1)$</p> <p>This is optimal for this problem.</p> <hr/> <h2 id="8-edge-cases">8. Edge Cases</h2> <h3 id="81-all-non-negative-numbers">8.1. All Non-Negative Numbers</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>
</code></pre></div></div> <ul> <li>Squaring keeps the array sorted: <code class="language-plaintext highlighter-rouge">[0, 1, 4, 9]</code>.</li> <li>Our algorithm still works: <ul> <li><code class="language-plaintext highlighter-rouge">right</code> pointer will always provide the larger square,</li> <li>We fill from right to left,</li> <li>Result remains sorted.</li> </ul> </li> </ul> <h3 id="82-all-non-positive-numbers">8.2. All Non-Positive Numbers</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="o">-</span><span class="mi">7</span><span class="p">,</span> <span class="o">-</span><span class="mi">5</span><span class="p">,</span> <span class="o">-</span><span class="mi">3</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">]</span>
</code></pre></div></div> <ul> <li>Squaring yields <code class="language-plaintext highlighter-rouge">[49, 25, 9, 1]</code> (which is descending).</li> <li>Our algorithm: <ul> <li>Takes larger squares from the <strong>left</strong> side (most negative),</li> <li>Fills the result from the <strong>end</strong>,</li> <li>Effectively reverses those squares into non-decreasing order: <code class="language-plaintext highlighter-rouge">[1, 9, 25, 49]</code>.</li> </ul> </li> </ul> <h3 id="83-mixed-negatives-and-positives">8.3. Mixed Negatives and Positives</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="o">-</span><span class="mi">3</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">]</span>
</code></pre></div></div> <ul> <li>The two-pointer logic correctly captures which side has the larger absolute value at each step.</li> <li>No special cases needed; the algorithm is uniform.</li> </ul> <h3 id="84-single-element">8.4. Single Element</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">5</span><span class="p">]</span>      <span class="c1"># or [-5]
</span></code></pre></div></div> <ul> <li>Only one value; its square is simply placed at <code class="language-plaintext highlighter-rouge">result[0]</code>.</li> </ul> <h3 id="85-empty-array">8.5. Empty Array</h3> <p>Even though LeetCode usually doesn’t give empty input for this problem:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[]</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">n = 0</code>, <code class="language-plaintext highlighter-rouge">result = []</code>,</li> <li>The loop doesn’t run,</li> <li><code class="language-plaintext highlighter-rouge">result</code> remains <code class="language-plaintext highlighter-rouge">[]</code>.</li> </ul> <p>The algorithm gracefully handles this case.</p> <hr/> <h2 id="9-common-interview-mistakes">9. Common Interview Mistakes</h2> <p>Even with such a structured problem, candidates often fall into a few traps.</p> <h3 id="91-ignoring-the-inputs-sorted-property">9.1. Ignoring the Input’s Sorted Property</h3> <p>Doing:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">squares</span> <span class="o">=</span> <span class="p">[</span><span class="n">x</span> <span class="o">*</span> <span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">nums</span><span class="p">]</span>
<span class="n">squares</span><span class="p">.</span><span class="nf">sort</span><span class="p">()</span>
<span class="k">return</span> <span class="n">squares</span>
</code></pre></div></div> <p>This works, but:</p> <ul> <li>Runs in <code class="language-plaintext highlighter-rouge">O(n log n)</code>,</li> <li>Fails to leverage the sorted nature of the input,</li> <li>Is considered suboptimal in an interview context when a linear solution exists.</li> </ul> <p>Interviewers want to see whether you notice and <strong>exploit problem-specific structure</strong>.</p> <h3 id="92-mishandling-indices">9.2. Mishandling Indices</h3> <p>Off-by-one errors are common:</p> <ul> <li>Incorrect start or end indices in the loop,</li> <li>Wrong range in <code class="language-plaintext highlighter-rouge">range(...)</code>,</li> <li>Mis-ordered updates to <code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code>.</li> </ul> <p>The pattern <code class="language-plaintext highlighter-rouge">for k in range(n - 1, -1, -1)</code> and condition <code class="language-plaintext highlighter-rouge">left &lt;= right</code> (implicitly via loop index) helps keep things clean.</p> <h3 id="93-filling-from-the-front">9.3. Filling from the Front</h3> <p>Some candidates try to fill the result from the front:</p> <ul> <li>This is harder because you don’t know where to place the smaller squares if you haven’t finished placing larger ones.</li> <li>Filling from the <strong>back</strong> matches naturally with always choosing the <strong>current largest</strong> square.</li> </ul> <h3 id="94-overcomplicating-the-logic">9.4. Overcomplicating the Logic</h3> <p>Adding extra conditions, nested loops, or temporary arrays is a red flag:</p> <ul> <li>This problem has a very clean solution,</li> <li>Any unnecessary complexity suggests discomfort with two-pointer patterns.</li> </ul> <hr/> <h2 id="10-how-this-connects-to-other-problems">10. How This Connects to Other Problems</h2> <p>This problem is not just about squares; it’s about patterns.</p> <p>The same two-pointer-from-both-ends idea appears in:</p> <ul> <li><strong>LeetCode 88 – Merge Sorted Array</strong> <ul> <li>Filling from the back while merging two sorted arrays.</li> </ul> </li> <li><strong>LeetCode 283 – Move Zeroes</strong> <ul> <li>Two pointers to compact and reorder elements in place.</li> </ul> </li> <li><strong>LeetCode 344 – Reverse String</strong> <ul> <li>Two pointers swapping from both ends towards the middle.</li> </ul> </li> <li>Palindrome checks, partitioning arrays, and more.</li> </ul> <p>Mental pattern to remember:</p> <blockquote> <p>“When you need to build a result array in sorted order and the largest elements are accessible from the ends, consider <strong>two pointers</strong> and <strong>fill from the back</strong>.”</p> </blockquote> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="Array"/><category term="TwoPointers"/><category term="Sorting"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Squares of a Sorted Array]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0344</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0344/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0344"/><published>2026-05-05T14:10:00+00:00</published><updated>2026-05-05T14:10:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0344</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0344/"><![CDATA[<h2 id="problem-0344-reverse-string">Problem 0344: Reverse String</h2> <p>Reversing a string sounds like the easiest task in the world—until the problem explicitly says:</p> <ul> <li>The string is given as a <strong>character array</strong>.</li> <li>You must reverse it <strong>in-place</strong>.</li> <li>You must use <strong>O(1) extra memory</strong>.</li> </ul> <p>That’s exactly what <strong>LeetCode 344 – Reverse String</strong> asks you to do.<br/> While this problem is considered “easy”, interviewers love it because it tests:</p> <ul> <li>Whether you respect <strong>constraints</strong> (in-place, constant space),</li> <li>How comfortable you are with the <strong>two-pointer pattern</strong>,</li> <li>Whether you avoid unnecessary complexity or library shortcuts.</li> </ul> <p>In this post, we’ll cover:</p> <ul> <li>Problem statement in simple terms</li> <li>Why the obvious “Pythonic” solution is <em>not</em> what they want</li> <li>The two-pointer in-place strategy</li> <li>A clean Python implementation</li> <li>Step-by-step walkthrough</li> <li>Time and space complexity</li> <li>Common mistakes in interviews</li> <li>Key mental patterns you can reuse in other problems</li> </ul> <hr/> <h2 id="1-problem-statement">1. Problem Statement</h2> <blockquote> <p>Given a character array <code class="language-plaintext highlighter-rouge">s</code>, reverse the array <strong>in-place</strong>.</p> </blockquote> <p>Formal constraints:</p> <ul> <li><code class="language-plaintext highlighter-rouge">s</code> is a list of characters, e.g. <code class="language-plaintext highlighter-rouge">["h", "e", "l", "l", "o"]</code>.</li> <li>You must: <ul> <li>Modify <code class="language-plaintext highlighter-rouge">s</code> directly.</li> <li>Use <strong>O(1) extra memory</strong>.</li> <li>Not return a new array.</li> </ul> </li> </ul> <h3 id="11-example-1">1.1. Example 1</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = ["h","e","l","l","o"]
Output: ["o","l","l","e","h"]
</code></pre></div></div> <h3 id="12-example-2">1.2. Example 2</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  s = ["H","a","n","n","a","h"]
Output: ["h","a","n","n","a","H"]
</code></pre></div></div> <p>Simple enough. But let’s look at what <em>not</em> to do first.</p> <hr/> <h2 id="2-first-attempt-why-simple-python-tricks-arent-enough">2. First Attempt: Why Simple Python Tricks Aren’t Enough</h2> <p>If you ignore the constraints, Python gives you multiple easy ways to reverse a sequence:</p> <h3 id="21-using-slicing">2.1. Using Slicing</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span><span class="p">[:]</span> <span class="o">=</span> <span class="n">s</span><span class="p">[::</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
</code></pre></div></div> <p>Or even:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span><span class="p">.</span><span class="nf">reverse</span><span class="p">()</span>
</code></pre></div></div> <p>Both of these:</p> <ul> <li>Reverse the array.</li> <li>Are concise and readable.</li> </ul> <p>So why not just use them?</p> <p>Because in many interview settings, and especially on LeetCode:</p> <ul> <li>The goal is to test your <strong>algorithmic thinking</strong>, not your knowledge of built-in methods.</li> <li>They explicitly want you to show: <ul> <li>How you handle <strong>two-pointer</strong> techniques.</li> <li>How you implement <strong>in-place</strong> logic without extra memory.</li> </ul> </li> </ul> <p>Also, if you move to lower-level languages (C, C++, Java), these shortcuts don’t exist. The underlying idea you need is the same everywhere: <strong>swap from both ends towards the middle</strong>.</p> <hr/> <h2 id="3-core-idea-two-pointers-from-both-ends">3. Core Idea: Two Pointers from Both Ends</h2> <p>To reverse an array in-place, the natural strategy is:</p> <ul> <li>Swap the first and last elements.</li> <li>Then the second and second-to-last.</li> <li>Continue until you reach the middle.</li> </ul> <p>This is exactly the <strong>two-pointer pattern</strong>:</p> <ul> <li><code class="language-plaintext highlighter-rouge">left</code> pointer: <ul> <li>Starts at the beginning (<code class="language-plaintext highlighter-rouge">0</code>).</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">right</code> pointer: <ul> <li>Starts at the end (<code class="language-plaintext highlighter-rouge">len(s) - 1</code>).</li> </ul> </li> <li>While <code class="language-plaintext highlighter-rouge">left &lt; right</code>: <ul> <li>Swap <code class="language-plaintext highlighter-rouge">s[left]</code> and <code class="language-plaintext highlighter-rouge">s[right]</code>.</li> <li>Move <code class="language-plaintext highlighter-rouge">left</code> forward.</li> <li>Move <code class="language-plaintext highlighter-rouge">right</code> backward.</li> </ul> </li> </ul> <p>At the end:</p> <ul> <li>All characters are reversed,</li> <li>No extra array is used,</li> <li>The operation is in-place and efficient.</li> </ul> <hr/> <h2 id="4-python-implementation-two-pointer-in-place">4. Python Implementation (Two-Pointer In-Place)</h2> <p>Here’s a clean and LeetCode-compatible implementation in Python:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">reverseString</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">s</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">str</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Reverses the input list of characters in-place.

        :param s: List of characters representing a string
        :return: None (modifies s in-place)
        </span><span class="sh">"""</span>

        <span class="n">left</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span>

        <span class="c1"># Move two pointers towards the center
</span>        <span class="k">while</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">:</span>
            <span class="c1"># Swap characters at left and right
</span>            <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">]</span> <span class="o">=</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">],</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">]</span>

            <span class="c1"># Move pointers inward
</span>            <span class="n">left</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="n">right</span> <span class="o">-=</span> <span class="mi">1</span>
</code></pre></div></div> <h3 id="41-key-points">4.1. Key points:</h3> <ul> <li>The method returns <code class="language-plaintext highlighter-rouge">None</code> because: <ul> <li>The function modifies <code class="language-plaintext highlighter-rouge">s</code> directly.</li> <li>LeetCode checks the final state of <code class="language-plaintext highlighter-rouge">s</code>.</li> </ul> </li> <li>The algorithm uses: <ul> <li>A <strong>single loop</strong>.</li> <li>Only <strong>two indices</strong> (<code class="language-plaintext highlighter-rouge">left</code>, <code class="language-plaintext highlighter-rouge">right</code>).</li> <li>No extra arrays.</li> </ul> </li> </ul> <p>This is exactly what interviewers want to see: simple, correct, and constraint-aware.</p> <hr/> <h2 id="5-step-by-step-walkthrough">5. Step-by-Step Walkthrough</h2> <p>Let’s dry-run the function on:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">h</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">e</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">l</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">l</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">o</span><span class="sh">"</span><span class="p">]</span>
</code></pre></div></div> <p>Initial:</p> <ul> <li><code class="language-plaintext highlighter-rouge">s = ["h","e","l","l","o"]</code></li> <li><code class="language-plaintext highlighter-rouge">left = 0</code> → <code class="language-plaintext highlighter-rouge">"h"</code></li> <li><code class="language-plaintext highlighter-rouge">right = 4</code> → <code class="language-plaintext highlighter-rouge">"o"</code></li> </ul> <h3 id="51-iteration-1">5.1. Iteration 1</h3> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = 4</code></li> <li><code class="language-plaintext highlighter-rouge">s[left] = "h"</code>, <code class="language-plaintext highlighter-rouge">s[right] = "o"</code></li> <li>Swap them:</li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>s = ["o","e","l","l","h"]
</code></pre></div></div> <ul> <li>Move pointers:</li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>left = 1
right = 3
</code></pre></div></div> <h3 id="52-iteration-2">5.2. Iteration 2</h3> <ul> <li><code class="language-plaintext highlighter-rouge">left = 1</code>, <code class="language-plaintext highlighter-rouge">right = 3</code></li> <li><code class="language-plaintext highlighter-rouge">s[1] = "e"</code>, <code class="language-plaintext highlighter-rouge">s[3] = "l"</code></li> <li>Swap:</li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>s = ["o","l","l","e","h"]
</code></pre></div></div> <ul> <li>Move pointers:</li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>left = 2
right = 2
</code></pre></div></div> <h3 id="53-iteration-3">5.3. Iteration 3</h3> <ul> <li>Now <code class="language-plaintext highlighter-rouge">left = 2</code>, <code class="language-plaintext highlighter-rouge">right = 2</code></li> <li>Condition <code class="language-plaintext highlighter-rouge">left &lt; right</code> is <strong>false</strong>.</li> <li>Loop ends.</li> </ul> <p>Final array:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>["o","l","l","e","h"]
</code></pre></div></div> <p>The string is correctly reversed, and the operation was done entirely <strong>in-place</strong>.</p> <hr/> <h2 id="6-edge-cases-and-how-the-algorithm-handles-them">6. Edge Cases and How the Algorithm Handles Them</h2> <p>A good interview answer always shows you’ve thought about edge cases.</p> <h3 id="61-empty-array">6.1. Empty Array</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="p">[]</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = len(s) - 1 = -1</code></li> <li><code class="language-plaintext highlighter-rouge">left &lt; right</code> → <code class="language-plaintext highlighter-rouge">0 &lt; -1</code> is false.</li> <li>The loop never runs.</li> <li><code class="language-plaintext highlighter-rouge">s</code> stays <code class="language-plaintext highlighter-rouge">[]</code>, which is correct.</li> </ul> <h3 id="62-single-character">6.2. Single Character</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="p">]</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0</code>, <code class="language-plaintext highlighter-rouge">right = 0</code></li> <li><code class="language-plaintext highlighter-rouge">left &lt; right</code> → <code class="language-plaintext highlighter-rouge">0 &lt; 0</code> is false.</li> <li>No swaps are needed.</li> <li>The array remains <code class="language-plaintext highlighter-rouge">["a"]</code>.</li> </ul> <h3 id="63-even-length-string">6.3. Even Length String</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">b</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">c</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">d</span><span class="sh">"</span><span class="p">]</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">left = 0, right = 3</code> → swap <code class="language-plaintext highlighter-rouge">"a"</code> and <code class="language-plaintext highlighter-rouge">"d"</code></li> <li><code class="language-plaintext highlighter-rouge">left = 1, right = 2</code> → swap <code class="language-plaintext highlighter-rouge">"b"</code> and <code class="language-plaintext highlighter-rouge">"c"</code></li> <li><code class="language-plaintext highlighter-rouge">left = 2, right = 1</code> → loop ends</li> <li>Final: <code class="language-plaintext highlighter-rouge">["d","c","b","a"]</code></li> </ul> <h3 id="64-palindrome">6.4. Palindrome</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">r</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">c</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">e</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">c</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">r</span><span class="sh">"</span><span class="p">]</span>
</code></pre></div></div> <ul> <li>Reversing a palindrome yields the same sequence.</li> <li>Algorithm runs, but final array equals the original.</li> <li>Shows the algorithm is general and doesn’t rely on special properties.</li> </ul> <hr/> <h2 id="7-time-and-space-complexity">7. Time and Space Complexity</h2> <h3 id="71-time-complexity">7.1. Time Complexity</h3> <ul> <li>We use two pointers that move towards each other.</li> <li>At each step, they move closer by 1 position each.</li> <li>The number of iterations is approximately <code class="language-plaintext highlighter-rouge">n / 2</code> for an array of length <code class="language-plaintext highlighter-rouge">n</code>.</li> </ul> <p>So the time complexity is:</p> <p>$\text{Time Complexity} = O(n)$</p> <h3 id="72-space-complexity">7.2. Space Complexity</h3> <ul> <li>We use only a constant number of extra variables (<code class="language-plaintext highlighter-rouge">left</code>, <code class="language-plaintext highlighter-rouge">right</code>).</li> <li>Swaps are done in-place without extra arrays.</li> </ul> <p>So the space complexity is:</p> <p>$\text{Space Complexity} = O(1)$</p> <p>This exactly satisfies the problem requirements.</p> <hr/> <h2 id="8-common-mistakes-in-interviews">8. Common Mistakes in Interviews</h2> <p>Even though this is an “easy” problem, a surprising number of candidates make mistakes. Here are some of the most common ones.</p> <h3 id="81-returning-a-new-array">8.1. Returning a New Array</h3> <p>Example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">reverseString</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">s</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">str</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="k">return</span> <span class="n">s</span><span class="p">[::</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
</code></pre></div></div> <p>This is incorrect for two reasons:</p> <ul> <li>The function is supposed to <strong>return <code class="language-plaintext highlighter-rouge">None</code></strong>, not a new list.</li> <li>The problem requires <strong>in-place</strong> modification of <code class="language-plaintext highlighter-rouge">s</code>.</li> </ul> <p>Interviewers care a lot about whether you read and respect the constraints.</p> <h3 id="82-using-extra-data-structures">8.2. Using Extra Data Structures</h3> <p>Something like:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">temp</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">ch</span> <span class="ow">in</span> <span class="n">s</span><span class="p">:</span>
    <span class="n">temp</span><span class="p">.</span><span class="nf">insert</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">ch</span><span class="p">)</span>
<span class="n">s</span><span class="p">[:]</span> <span class="o">=</span> <span class="n">temp</span>
</code></pre></div></div> <p>Issues:</p> <ul> <li>Uses <strong>O(n)</strong> extra memory (<code class="language-plaintext highlighter-rouge">temp</code>).</li> <li>Although it “works”, it violates the <strong>O(1) extra space</strong> requirement.</li> <li>Also less efficient than necessary.</li> </ul> <h3 id="83-off-by-one-errors-with-indices">8.3. Off-by-One Errors with Indices</h3> <p>Using incorrect boundaries, e.g.:</p> <ul> <li>Running the loop while <code class="language-plaintext highlighter-rouge">left &lt;= right</code> and mishandling the middle.</li> <li>Miscomputing <code class="language-plaintext highlighter-rouge">right</code> or starting <code class="language-plaintext highlighter-rouge">left</code> at 1 instead of 0.</li> </ul> <p>Two-pointer problems are a classic source of off-by-one bugs. A clean condition like <code class="language-plaintext highlighter-rouge">while left &lt; right</code> helps avoid these.</p> <h3 id="84-overcomplicating-the-solution">8.4. Overcomplicating the Solution</h3> <p>Sometimes candidates:</p> <ul> <li>Use multiple loops,</li> <li>Introduce unnecessary temporary arrays,</li> <li>Or add complex conditions.</li> </ul> <p>For a simple reversal, this is a red flag. Interviewers expect you to choose the <strong>simplest correct solution</strong>.</p> <hr/> <h2 id="9-why-this-problem-matters">9. Why This Problem Matters</h2> <p>You might think:</p> <blockquote> <p>“It’s just reversing a string. Why do interviewers care?”</p> </blockquote> <p>Because this problem tests core fundamentals:</p> <ul> <li><strong>Two-pointer pattern</strong>: <ul> <li>Left and right pointers moving towards each other.</li> </ul> </li> <li><strong>In-place</strong> operations: <ul> <li>Modifying input without extra memory.</li> </ul> </li> <li><strong>Index management</strong>: <ul> <li>Avoiding off-by-one and boundary errors.</li> </ul> </li> <li><strong>Constraint awareness</strong>: <ul> <li>Not falling back to high-level helpers when the problem clearly wants low-level thinking.</li> </ul> </li> </ul> <p>Once you internalize this pattern, you’ll see it pop up in many other problems:</p> <ul> <li>Checking if a string is a <strong>palindrome</strong>.</li> <li>Reversing parts of an array (e.g., rotate array segments).</li> <li>Partitioning arrays around a pivot.</li> <li>Merging or comparing from both ends.</li> </ul> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="TwoPointers"/><category term="String"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Reverse String]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0283</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0283/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0283"/><published>2026-05-04T14:50:00+00:00</published><updated>2026-05-04T14:50:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0283</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0283/"><![CDATA[<h2 id="problem-0283-move-zeroes">Problem 0283: Move Zeroes</h2> <p>Array manipulation problems on LeetCode often look deceptively simple — until you add constraints like:</p> <ul> <li>“Do it <strong>in-place</strong>”</li> <li>“Preserve the <strong>relative order</strong> of elements”</li> <li>“Use <strong>O(1) extra space</strong>”</li> </ul> <p><strong>LeetCode 283 – Move Zeroes</strong> is a perfect example. The task sounds straightforward:</p> <blockquote> <p>Move all zeroes to the end of the array while keeping the relative order of the non-zero elements.</p> </blockquote> <p>But designing a clean, efficient solution that works in-place and passes all edge cases is exactly what interviewers care about.</p> <p>In this post, we’ll walk through:</p> <ul> <li>The problem statement in simple terms</li> <li>Why a naïve solution is not ideal</li> <li>The core idea: <strong>two-pointer in-place compaction</strong></li> <li>A clean Python implementation</li> <li>Step-by-step walkthrough</li> <li>Time &amp; space complexity</li> <li>Common interview mistakes and how to avoid them</li> <li>Key takeaways for real interviews</li> </ul> <hr/> <h2 id="1-problem-overview">1. Problem Overview</h2> <p>You are given an array of integers <code class="language-plaintext highlighter-rouge">nums</code>. Your goal:</p> <blockquote> <p>Move all <code class="language-plaintext highlighter-rouge">0</code>s to the end of the array, <strong>without</strong> changing the relative order of the non-zero elements.</p> </blockquote> <h3 id="11-constraints">1.1. Constraints:</h3> <ul> <li>You must <strong>modify the array in-place</strong>.</li> <li>You should aim for <strong>O(n)</strong> time and <strong>O(1)</strong> extra space.</li> <li>You must <strong>not</strong> return a new array — just update the input.</li> </ul> <h3 id="12-example-1">1.2. Example 1</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  nums = [0, 1, 0, 3, 12]
Output: [1, 3, 12, 0, 0]
</code></pre></div></div> <p>The non-zero elements <code class="language-plaintext highlighter-rouge">[1, 3, 12]</code> keep their relative order, and all zeroes are moved to the end.</p> <h3 id="13-example-2">1.3. Example 2</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  nums = [0]
Output: [0]
</code></pre></div></div> <p>Nothing to move; the array stays the same.</p> <hr/> <h2 id="2-first-ideas-why-the-naïve-approach-isnt-great">2. First Ideas: Why the Naïve Approach Isn’t Great</h2> <p>If there were no constraints about in-place modification or extra space, a very simple strategy might be:</p> <ol> <li>Build a new array with all non-zero elements.</li> <li>Count how many zeroes there were.</li> <li>Append that many zeroes to the end.</li> </ol> <p>Something like:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">non_zeros</span> <span class="o">=</span> <span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">nums</span> <span class="k">if</span> <span class="n">x</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">]</span>
<span class="n">zeros_count</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span> <span class="o">-</span> <span class="nf">len</span><span class="p">(</span><span class="n">non_zeros</span><span class="p">)</span>
<span class="n">result</span> <span class="o">=</span> <span class="n">non_zeros</span> <span class="o">+</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">zeros_count</span>
</code></pre></div></div> <p>This works logically, but:</p> <ul> <li>It <strong>allocates extra memory</strong> proportional to <code class="language-plaintext highlighter-rouge">n</code> (the array length).</li> <li>It does <strong>not</strong> modify the input array in-place.</li> <li>In a real interview and on LeetCode, this usually fails the constraints.</li> </ul> <p>We need something better: <strong>in-place, O(1) extra space, and still O(n) time</strong>.</p> <hr/> <h2 id="3-key-insight-in-place-compaction-with-two-pointers">3. Key Insight: In-Place Compaction with Two Pointers</h2> <p>To satisfy all constraints, we want to:</p> <ul> <li><strong>Pull all non-zero elements to the front</strong> of the array.</li> <li>Maintain their <strong>relative order</strong>.</li> <li>Let all zeroes naturally “fall” to the back.</li> </ul> <p>This is a classic application of the <strong>two-pointer pattern</strong>:</p> <ul> <li>One pointer scans the array.</li> <li>One pointer tracks where to write the next valid element.</li> </ul> <h3 id="31-pointer-roles">3.1. Pointer Roles</h3> <p>Let’s define:</p> <ul> <li><code class="language-plaintext highlighter-rouge">j</code> – the <strong>scan pointer</strong> that iterates over every index from <code class="language-plaintext highlighter-rouge">0</code> to <code class="language-plaintext highlighter-rouge">n - 1</code>.</li> <li><code class="language-plaintext highlighter-rouge">i</code> – the <strong>write pointer</strong>, indicating the position where the next non-zero element should go.</li> </ul> <p>The algorithm:</p> <ol> <li>Initialize <code class="language-plaintext highlighter-rouge">i = 0</code>.</li> <li>For each <code class="language-plaintext highlighter-rouge">j</code> from <code class="language-plaintext highlighter-rouge">0</code> to <code class="language-plaintext highlighter-rouge">len(nums) - 1</code>: <ul> <li>If <code class="language-plaintext highlighter-rouge">nums[j]</code> is <strong>non-zero</strong>: <ul> <li>Swap <code class="language-plaintext highlighter-rouge">nums[i]</code> and <code class="language-plaintext highlighter-rouge">nums[j]</code>.</li> <li>Increment <code class="language-plaintext highlighter-rouge">i</code> (because we placed a non-zero element at position <code class="language-plaintext highlighter-rouge">i</code>).</li> </ul> </li> </ul> </li> <li>When done, all non-zero elements are compacted at the beginning, and all zeroes are at the end.</li> </ol> <h3 id="32-why-this-preserves-order">3.2. Why This Preserves Order</h3> <p>Notice:</p> <ul> <li>We scan from <strong>left to right</strong>.</li> <li>The first non-zero we see goes to position <code class="language-plaintext highlighter-rouge">0</code>.</li> <li>The second non-zero goes to position <code class="language-plaintext highlighter-rouge">1</code>.</li> <li>And so on…</li> </ul> <p>We never reorder non-zero elements relative to each other; we just move them forward, keeping their original sequence.</p> <hr/> <h2 id="4-python-implementation-in-place-two-pointer-solution">4. Python Implementation (In-Place Two-Pointer Solution)</h2> <p>Here’s a clean and interview-ready implementation in Python, based on the algorithm described above:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">moveZeroes</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">nums</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Moves all zeros in the list to the end while preserving
        the relative order of non-zero elements.

        This function modifies nums in-place and returns None.
        </span><span class="sh">"""</span>

        <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span>  <span class="c1"># index to place the next non-zero element
</span>
        <span class="c1"># j scans through the entire array
</span>        <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">nums</span><span class="p">)):</span>
            <span class="c1"># When we find a non-zero, we want it at index i
</span>            <span class="k">if</span> <span class="n">nums</span><span class="p">[</span><span class="n">j</span><span class="p">]</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">:</span>
                <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">nums</span><span class="p">[</span><span class="n">j</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">j</span><span class="p">],</span> <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>
                <span class="n">i</span> <span class="o">+=</span> <span class="mi">1</span>
</code></pre></div></div> <p>This matches the logic of your solution, with the correct method signature for LeetCode (<code class="language-plaintext highlighter-rouge">self</code> included) and no unnecessary changes to the algorithm.</p> <hr/> <h2 id="5-step-by-step-walkthrough">5. Step-by-Step Walkthrough</h2> <p>Let’s take the example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">12</span><span class="p">]</span>
</code></pre></div></div> <p>Initial:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i = 0</code></li> <li><code class="language-plaintext highlighter-rouge">j</code> will go from <code class="language-plaintext highlighter-rouge">0</code> to <code class="language-plaintext highlighter-rouge">4</code></li> </ul> <h3 id="51-iteration-1-j--0">5.1. Iteration 1: <code class="language-plaintext highlighter-rouge">j = 0</code></h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums[j] = nums[0] = 0</code></li> <li>It is zero → do nothing</li> <li><code class="language-plaintext highlighter-rouge">i</code> stays <code class="language-plaintext highlighter-rouge">0</code></li> <li>Array remains: <code class="language-plaintext highlighter-rouge">[0, 1, 0, 3, 12]</code></li> </ul> <h3 id="52-iteration-2-j--1">5.2. Iteration 2: <code class="language-plaintext highlighter-rouge">j = 1</code></h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums[1] = 1</code> (non-zero)</li> <li>Swap <code class="language-plaintext highlighter-rouge">nums[i]</code> and <code class="language-plaintext highlighter-rouge">nums[j]</code> → swap <code class="language-plaintext highlighter-rouge">nums[0]</code> and <code class="language-plaintext highlighter-rouge">nums[1]</code></li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums = [1, 0, 0, 3, 12]
i = 1
</code></pre></div></div> <p>We’ve placed <code class="language-plaintext highlighter-rouge">1</code> at the front.</p> <h3 id="53-iteration-3-j--2">5.3. Iteration 3: <code class="language-plaintext highlighter-rouge">j = 2</code></h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums[2] = 0</code></li> <li>It is zero → do nothing</li> <li><code class="language-plaintext highlighter-rouge">i</code> remains <code class="language-plaintext highlighter-rouge">1</code></li> <li>Array: <code class="language-plaintext highlighter-rouge">[1, 0, 0, 3, 12]</code></li> </ul> <h3 id="54-iteration-4-j--3">5.4. Iteration 4: <code class="language-plaintext highlighter-rouge">j = 3</code></h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums[3] = 3</code> (non-zero)</li> <li>Swap <code class="language-plaintext highlighter-rouge">nums[i]</code> and <code class="language-plaintext highlighter-rouge">nums[j]</code> → swap <code class="language-plaintext highlighter-rouge">nums[1]</code> and <code class="language-plaintext highlighter-rouge">nums[3]</code></li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums = [1, 3, 0, 0, 12]
i = 2
</code></pre></div></div> <p>Now <code class="language-plaintext highlighter-rouge">1</code> and <code class="language-plaintext highlighter-rouge">3</code> (first two non-zeros) are at positions <code class="language-plaintext highlighter-rouge">0</code> and <code class="language-plaintext highlighter-rouge">1</code>.</p> <h3 id="55-iteration-5-j--4">5.5. Iteration 5: <code class="language-plaintext highlighter-rouge">j = 4</code></h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums[4] = 12</code> (non-zero)</li> <li>Swap <code class="language-plaintext highlighter-rouge">nums[i]</code> and <code class="language-plaintext highlighter-rouge">nums[j]</code> → swap <code class="language-plaintext highlighter-rouge">nums[2]</code> and <code class="language-plaintext highlighter-rouge">nums[4]</code></li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums = [1, 3, 12, 0, 0]
i = 3
</code></pre></div></div> <p>Loop ends.</p> <p>Final array:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1, 3, 12, 0, 0]
</code></pre></div></div> <p>All non-zero elements are at the beginning, preserving their original order <code class="language-plaintext highlighter-rouge">[1, 3, 12]</code>.<br/> All zeroes are at the end.</p> <hr/> <h2 id="6-edge-cases">6. Edge Cases</h2> <p>A robust solution should handle edge cases gracefully. Let’s look at a few.</p> <h3 id="61-all-zeroes">6.1. All Zeroes</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
</code></pre></div></div> <ul> <li>The <code class="language-plaintext highlighter-rouge">if nums[j] != 0</code> condition is never true.</li> <li><code class="language-plaintext highlighter-rouge">i</code> stays <code class="language-plaintext highlighter-rouge">0</code>.</li> <li>The array stays <code class="language-plaintext highlighter-rouge">[0, 0, 0]</code> — which is already correct.</li> </ul> <h3 id="62-no-zeroes">6.2. No Zeroes</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>
</code></pre></div></div> <ul> <li>Every element is non-zero.</li> <li>For each <code class="language-plaintext highlighter-rouge">j</code>, we swap <code class="language-plaintext highlighter-rouge">nums[i]</code> with <code class="language-plaintext highlighter-rouge">nums[j]</code>, but since <code class="language-plaintext highlighter-rouge">i == j</code> at each step: <ul> <li>We are effectively swapping an element with itself.</li> </ul> </li> <li>The array stays <code class="language-plaintext highlighter-rouge">[1, 2, 3]</code>.</li> </ul> <p>This shows the algorithm doesn’t break or do unnecessary complex work when the array is already “ideal”.</p> <h3 id="63-zeroes-already-at-the-end">6.3. Zeroes Already at the End</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
</code></pre></div></div> <ul> <li>The non-zero part is already at the front.</li> <li>The algorithm will perform only self-swaps for <code class="language-plaintext highlighter-rouge">j = 0, 1, 2</code>, and ignore the zeros at <code class="language-plaintext highlighter-rouge">3</code> and <code class="language-plaintext highlighter-rouge">4</code>.</li> <li>The final array remains <code class="language-plaintext highlighter-rouge">[1, 2, 3, 0, 0]</code>.</li> </ul> <h3 id="64-single-element">6.4. Single Element</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span>   <span class="o">-&gt;</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span>
<span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">5</span><span class="p">]</span>   <span class="o">-&gt;</span> <span class="p">[</span><span class="mi">5</span><span class="p">]</span>
</code></pre></div></div> <p>Both are trivial but important to handle correctly. The loop runs once, and the logic still holds.</p> <hr/> <h2 id="7-time-and-space-complexity">7. Time and Space Complexity</h2> <h3 id="71-time-complexity">7.1. Time Complexity</h3> <p>We have:</p> <ul> <li>A single loop where <code class="language-plaintext highlighter-rouge">j</code> goes from <code class="language-plaintext highlighter-rouge">0</code> to <code class="language-plaintext highlighter-rouge">n - 1</code>, where <code class="language-plaintext highlighter-rouge">n = len(nums)</code>.</li> <li>Each element is processed <strong>exactly once</strong>.</li> <li>Swaps are constant time.</li> </ul> <p>So the overall time complexity is:</p> <p>$\text{Time Complexity} = O(n)$</p> <h3 id="72-space-complexity">7.2. Space Complexity</h3> <p>We use only:</p> <ul> <li>Two integer variables (<code class="language-plaintext highlighter-rouge">i</code> and <code class="language-plaintext highlighter-rouge">j</code>) in the process.</li> </ul> <p>No additional arrays or data structures are created, so the extra space used is constant:</p> <p>$\text{Space Complexity} = O(1)$</p> <p>This exactly matches the in-place, O(1) space requirement in the problem.</p> <hr/> <h2 id="8-common-interview-mistakes">8. Common Interview Mistakes</h2> <p>This problem is popular in interviews because it reveals how well you handle constraints and in-place logic. Here are some common pitfalls:</p> <h3 id="81-creating-a-new-array">8.1. Creating a New Array</h3> <p>A typical incorrect approach:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">non_zeros</span> <span class="o">=</span> <span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">nums</span> <span class="k">if</span> <span class="n">x</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">]</span>
<span class="n">zeros</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span> <span class="o">-</span> <span class="nf">len</span><span class="p">(</span><span class="n">non_zeros</span><span class="p">))</span>
<span class="n">nums</span> <span class="o">=</span> <span class="n">non_zeros</span> <span class="o">+</span> <span class="n">zeros</span>
</code></pre></div></div> <p>This may “work” in a vacuum, but:</p> <ul> <li>It does <strong>not</strong> modify the original <code class="language-plaintext highlighter-rouge">nums</code> in-place.</li> <li>It uses <strong>extra space</strong> proportional to <code class="language-plaintext highlighter-rouge">n</code>.</li> <li>On LeetCode and in interviews, this often fails the required constraints.</li> </ul> <h3 id="82-using-remove-and-append-repeatedly">8.2. Using <code class="language-plaintext highlighter-rouge">remove</code> and <code class="language-plaintext highlighter-rouge">append</code> Repeatedly</h3> <p>Another inefficient pattern:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">nums</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">x</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
        <span class="n">nums</span><span class="p">.</span><span class="nf">remove</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
        <span class="n">nums</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
</code></pre></div></div> <p>Issues:</p> <ul> <li><code class="language-plaintext highlighter-rouge">list.remove()</code> is <strong>O(n)</strong> because it has to shift elements.</li> <li>Combined with the loop, this often devolves into <strong>O(n²)</strong> time.</li> <li>It’s also error-prone due to changing the list while iterating over it.</li> </ul> <h3 id="83-destroying-the-relative-order">8.3. Destroying the Relative Order</h3> <p>Some candidates try to:</p> <ul> <li>Count zeros</li> <li>Filter non-zeros</li> <li>Then write non-zeros in arbitrary order</li> </ul> <p>If you don’t carefully maintain the original sequence of non-zero elements, the solution becomes <strong>incorrect</strong>, even if zeros are at the end.</p> <h3 id="84-overcomplicating-the-logic">8.4. Overcomplicating the Logic</h3> <p>Sometimes, solutions end up with:</p> <ul> <li>Multiple passes</li> <li>Many special cases</li> <li>Unnecessary conditions</li> </ul> <p>The beauty of the two-pointer approach is that it’s both <strong>simple and optimal</strong>.</p> <hr/> <h2 id="9-interview-takeaways">9. Interview Takeaways</h2> <p>LeetCode 283 – Move Zeroes is more than just an array cleanup problem. It’s a compact test of:</p> <ul> <li>Whether you think about <strong>in-place</strong> algorithms naturally</li> <li>How comfortable you are with the <strong>two-pointer pattern</strong></li> <li>Whether you pay attention to <strong>constraints</strong> like preserving order and O(1) extra space</li> </ul> <p>Patterns you reinforce here are directly applicable to other problems:</p> <ul> <li><strong>Remove Element</strong> (LeetCode 27)</li> <li><strong>Remove Duplicates from Sorted Array</strong> (LeetCode 26)</li> <li>General “filter in-place” or “compact array” tasks</li> <li>Partitioning arrays based on a condition</li> </ul> <p>Once you internalize the idea:</p> <blockquote> <p>“Use one pointer to scan, and another to write the next valid element.”</p> </blockquote> <p>you’ll find that a whole family of array problems becomes much easier.</p> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="Array"/><category term="TwoPointers"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Move Zeroes]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0088</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0088/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0088"/><published>2026-05-04T14:10:00+00:00</published><updated>2026-05-04T14:10:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0088</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0088/"><![CDATA[<h2 id="problem-0088-merge-sorted-array">Problem 0088: Merge Sorted Array</h2> <p>Array questions where everything is <em>almost</em> done for you — two sorted arrays, extra space already allocated — are exactly the kind of problems that interviewers use to test whether you can:</p> <ul> <li>Respect <strong>in-place</strong> constraints</li> <li>Use <strong>pointers intelligently</strong></li> <li>Avoid unnecessary extra memory</li> </ul> <p>LeetCode 88 – <strong>Merge Sorted Array</strong> – is one of the most fundamental problems in this category.</p> <p>In this post, we’ll cover:</p> <ul> <li>The problem in plain English</li> <li>Why a naïve merge can break the constraints</li> <li>The key idea: merging from the <strong>back</strong></li> <li>A clean Python solution using three pointers</li> <li>Step-by-step walkthrough with an example</li> <li>Time &amp; space complexity</li> <li>Common pitfalls and interview tips</li> </ul> <hr/> <h2 id="1-problem-overview-in-plain-english">1. Problem Overview (In Plain English)</h2> <p>You are given:</p> <ul> <li>A sorted array <code class="language-plaintext highlighter-rouge">nums1</code> of length <code class="language-plaintext highlighter-rouge">m + n</code> <ul> <li>The first <code class="language-plaintext highlighter-rouge">m</code> elements are <strong>valid</strong> and sorted</li> <li>The last <code class="language-plaintext highlighter-rouge">n</code> elements are just <strong>empty space</strong> (often filled with <code class="language-plaintext highlighter-rouge">0</code>s)</li> </ul> </li> <li>A sorted array <code class="language-plaintext highlighter-rouge">nums2</code> of length <code class="language-plaintext highlighter-rouge">n</code></li> <li>Two integers <code class="language-plaintext highlighter-rouge">m</code> and <code class="language-plaintext highlighter-rouge">n</code>, indicating how many meaningful elements each array contains</li> </ul> <p>Your task:</p> <blockquote> <p>Merge <code class="language-plaintext highlighter-rouge">nums2</code> into <code class="language-plaintext highlighter-rouge">nums1</code> as one sorted array, <strong>in-place</strong>.<br/> Do not return a new array — modify <code class="language-plaintext highlighter-rouge">nums1</code> directly.</p> </blockquote> <p>After the function finishes:</p> <ul> <li><code class="language-plaintext highlighter-rouge">nums1</code> should contain all <code class="language-plaintext highlighter-rouge">m + n</code> elements from both arrays, sorted.</li> <li>The function should return <code class="language-plaintext highlighter-rouge">None</code>.</li> </ul> <h3 id="11-example">1.1. Example</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:
nums1 = [1, 2, 3, 0, 0, 0], m = 3
nums2 = [2, 5, 6],          n = 3

Output:
nums1 = [1, 2, 2, 3, 5, 6]
</code></pre></div></div> <p>The extra <code class="language-plaintext highlighter-rouge">0</code>s in <code class="language-plaintext highlighter-rouge">nums1</code> are just placeholders. After merging, <code class="language-plaintext highlighter-rouge">nums1</code> becomes the fully sorted array of all 6 elements.</p> <hr/> <h2 id="2-first-thoughts-the-wrong-way-but-very-tempting">2. First Thoughts: The Wrong Way (But Very Tempting)</h2> <p>If there were no constraints, a very natural idea would be:</p> <ol> <li>Concatenate the two arrays: <code class="language-plaintext highlighter-rouge">nums1_valid + nums2</code></li> <li>Sort the result</li> <li>Return a new array</li> </ol> <p>Or in code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">merged</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span><span class="n">nums1</span><span class="p">[:</span><span class="n">m</span><span class="p">]</span> <span class="o">+</span> <span class="n">nums2</span><span class="p">)</span>
<span class="k">return</span> <span class="n">merged</span>
</code></pre></div></div> <p>But this breaks two important requirements of the problem:</p> <ol> <li>You must <strong>modify <code class="language-plaintext highlighter-rouge">nums1</code> in-place</strong></li> <li>You must not use <strong>extra space proportional to <code class="language-plaintext highlighter-rouge">m + n</code></strong></li> </ol> <p>In interviews, ignoring these constraints is often treated as a <strong>wrong solution</strong>, even if the output is technically correct.</p> <p>So we need a smarter strategy.</p> <hr/> <h2 id="3-key-insight-merge-from-the-back">3. Key Insight: Merge From the Back</h2> <p>We’re merging <strong>two sorted arrays</strong>. This is similar to the merge step in <strong>Merge Sort</strong>:</p> <ul> <li>Normally, we use two pointers from the <strong>front</strong> and build a result array.</li> <li>But here, we <strong>cannot</strong> create a separate result array.</li> </ul> <p>What makes this problem special:</p> <ul> <li><code class="language-plaintext highlighter-rouge">nums1</code> already has <strong>enough space at the end</strong> to hold all elements (<code class="language-plaintext highlighter-rouge">m + n</code>).</li> <li>The valid elements in <code class="language-plaintext highlighter-rouge">nums1</code> are only in the <strong>first <code class="language-plaintext highlighter-rouge">m</code> positions</strong>.</li> </ul> <p>If we try to merge from the <strong>front</strong>, we’ll end up overwriting values in <code class="language-plaintext highlighter-rouge">nums1</code> that we haven’t processed yet. That leads to data loss and messy shifting.</p> <p>So the trick is:</p> <blockquote> <p>Use three pointers and merge from the <strong>end</strong> of <code class="language-plaintext highlighter-rouge">nums1</code> backward.</p> </blockquote> <h3 id="31-three-pointers">3.1. Three Pointers</h3> <p>We define:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i = m - 1</code> → points to the <strong>last valid element</strong> in <code class="language-plaintext highlighter-rouge">nums1</code></li> <li><code class="language-plaintext highlighter-rouge">j = n - 1</code> → points to the <strong>last element</strong> in <code class="language-plaintext highlighter-rouge">nums2</code></li> <li><code class="language-plaintext highlighter-rouge">k = m + n - 1</code> → points to the <strong>last index</strong> of <code class="language-plaintext highlighter-rouge">nums1</code> (where we will write)</li> </ul> <p>At each step, we:</p> <ol> <li>Compare <code class="language-plaintext highlighter-rouge">nums1[i]</code> and <code class="language-plaintext highlighter-rouge">nums2[j]</code></li> <li>Place the <strong>larger</strong> one at position <code class="language-plaintext highlighter-rouge">k</code> in <code class="language-plaintext highlighter-rouge">nums1</code></li> <li>Move that pointer (<code class="language-plaintext highlighter-rouge">i</code> or <code class="language-plaintext highlighter-rouge">j</code>) to the left</li> <li>Move <code class="language-plaintext highlighter-rouge">k</code> to the left</li> </ol> <p>We repeat this until we’ve placed all elements of <code class="language-plaintext highlighter-rouge">nums2</code> into <code class="language-plaintext highlighter-rouge">nums1</code>.</p> <p>Why this works:</p> <ul> <li>We always write into the <strong>free space at the back</strong>.</li> <li>We never overwrite any meaningful value in <code class="language-plaintext highlighter-rouge">nums1</code> that we haven’t already used.</li> <li>We maintain sorted order naturally because we always choose the largest remaining element.</li> </ul> <hr/> <h2 id="4-python-solution-in-place-three-pointers">4. Python Solution (In-Place, Three Pointers)</h2> <p>Here’s a clean and interview-ready implementation:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">merge</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">nums1</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">],</span> <span class="n">m</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
              <span class="n">nums2</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">],</span> <span class="n">n</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Merge nums2 into nums1 as one sorted array in-place.

        Args:
            nums1: List[int] of length m + n, where:
                   - First m elements are valid and sorted.
                   - Last n elements are placeholders (e.g. 0).
            m: Number of valid elements in nums1.
            nums2: List[int] of length n, sorted.
            n: Number of elements in nums2.

        Returns:
            None. The result is stored in nums1 in-place.
        </span><span class="sh">"""</span>

        <span class="c1"># Pointer i: last valid element in nums1
</span>        <span class="n">i</span> <span class="o">=</span> <span class="n">m</span> <span class="o">-</span> <span class="mi">1</span>
        <span class="c1"># Pointer j: last element in nums2
</span>        <span class="n">j</span> <span class="o">=</span> <span class="n">n</span> <span class="o">-</span> <span class="mi">1</span>
        <span class="c1"># Pointer k: last position in nums1 (full length)
</span>        <span class="n">k</span> <span class="o">=</span> <span class="n">m</span> <span class="o">+</span> <span class="n">n</span> <span class="o">-</span> <span class="mi">1</span>

        <span class="c1"># Merge nums1 and nums2 starting from the end
</span>        <span class="k">while</span> <span class="n">j</span> <span class="o">&gt;=</span> <span class="mi">0</span><span class="p">:</span>
            <span class="c1"># If there are still elements in nums1 to compare
</span>            <span class="c1"># and the current element in nums1 is larger,
</span>            <span class="c1"># place nums1[i] at nums1[k].
</span>            <span class="k">if</span> <span class="n">i</span> <span class="o">&gt;=</span> <span class="mi">0</span> <span class="ow">and</span> <span class="n">nums1</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">&gt;</span> <span class="n">nums2</span><span class="p">[</span><span class="n">j</span><span class="p">]:</span>
                <span class="n">nums1</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums1</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>
                <span class="n">i</span> <span class="o">-=</span> <span class="mi">1</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="c1"># Otherwise, place nums2[j] at nums1[k].
</span>                <span class="n">nums1</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums2</span><span class="p">[</span><span class="n">j</span><span class="p">]</span>
                <span class="n">j</span> <span class="o">-=</span> <span class="mi">1</span>

            <span class="n">k</span> <span class="o">-=</span> <span class="mi">1</span>

        <span class="c1"># No return needed; nums1 is modified in-place.
</span></code></pre></div></div> <p>This is essentially your solution, Mohammad Hossein, with:</p> <ul> <li>The correct method signature (<code class="language-plaintext highlighter-rouge">self</code> included)</li> <li>No <code class="language-plaintext highlighter-rouge">print</code> inside the function</li> <li>Clear comments and docstring</li> </ul> <hr/> <h2 id="5-walkthrough-step-by-step-example">5. Walkthrough: Step-by-Step Example</h2> <p>Let’s walk through the main example to really lock in the intuition.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums1</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
<span class="n">m</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">nums2</span> <span class="o">=</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">]</span>
<span class="n">n</span> <span class="o">=</span> <span class="mi">3</span>
</code></pre></div></div> <p>Initial pointers:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i = m - 1 = 2</code> → <code class="language-plaintext highlighter-rouge">nums1[i] = 3</code></li> <li><code class="language-plaintext highlighter-rouge">j = n - 1 = 2</code> → <code class="language-plaintext highlighter-rouge">nums2[j] = 6</code></li> <li><code class="language-plaintext highlighter-rouge">k = m + n - 1 = 5</code> → <code class="language-plaintext highlighter-rouge">nums1[k]</code> (where we’ll write)</li> </ul> <h3 id="51-iteration-1">5.1. Iteration 1</h3> <ul> <li>Compare <code class="language-plaintext highlighter-rouge">nums1[i] = 3</code> and <code class="language-plaintext highlighter-rouge">nums2[j] = 6</code></li> <li><code class="language-plaintext highlighter-rouge">6</code> is larger → place it at <code class="language-plaintext highlighter-rouge">nums1[k]</code></li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums1[k] = nums2[j]  → nums1[5] = 6

nums1 = [1, 2, 3, 0, 0, 6]
j = 1
k = 4
</code></pre></div></div> <h3 id="52-iteration-2">5.2. Iteration 2</h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums1[i] = 3</code>, <code class="language-plaintext highlighter-rouge">nums2[j] = 5</code></li> <li><code class="language-plaintext highlighter-rouge">5</code> is larger → place it at <code class="language-plaintext highlighter-rouge">nums1[4]</code></li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums1[4] = 5

nums1 = [1, 2, 3, 0, 5, 6]
j = 0
k = 3
</code></pre></div></div> <h3 id="53-iteration-3">5.3. Iteration 3</h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums1[i] = 3</code>, <code class="language-plaintext highlighter-rouge">nums2[j] = 2</code></li> <li><code class="language-plaintext highlighter-rouge">3</code> is larger → place it at <code class="language-plaintext highlighter-rouge">nums1[3]</code></li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums1[3] = 3

nums1 = [1, 2, 3, 3, 5, 6]
i = 1
k = 2
</code></pre></div></div> <h3 id="54-iteration-4">5.4. Iteration 4</h3> <ul> <li><code class="language-plaintext highlighter-rouge">nums1[i] = 2</code>, <code class="language-plaintext highlighter-rouge">nums2[j] = 2</code></li> <li>They are equal; we can choose <code class="language-plaintext highlighter-rouge">nums2[j]</code> (or <code class="language-plaintext highlighter-rouge">nums1[i]</code>, both fine)</li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums1[2] = nums2[0] = 2

nums1 = [1, 2, 2, 3, 5, 6]
j = -1
k = 1
</code></pre></div></div> <p>Loop condition: <code class="language-plaintext highlighter-rouge">while j &gt;= 0</code> → now <code class="language-plaintext highlighter-rouge">j = -1</code>, so we stop.</p> <p>At this point:</p> <ul> <li>All elements of <code class="language-plaintext highlighter-rouge">nums2</code> have been merged into <code class="language-plaintext highlighter-rouge">nums1</code>.</li> <li>Any remaining elements on the left side of <code class="language-plaintext highlighter-rouge">nums1</code> (indexes <code class="language-plaintext highlighter-rouge">0..i</code>) are already in the correct place.</li> </ul> <p>Final result:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums1 = [1, 2, 2, 3, 5, 6]
</code></pre></div></div> <p>Exactly what we want.</p> <hr/> <h2 id="6-edge-cases-to-consider">6. Edge Cases to Consider</h2> <h3 id="61-nums2-is-empty-n--0">6.1. <code class="language-plaintext highlighter-rouge">nums2</code> is empty (<code class="language-plaintext highlighter-rouge">n = 0</code>)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums1</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>
<span class="n">m</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">nums2</span> <span class="o">=</span> <span class="p">[]</span>
<span class="n">n</span> <span class="o">=</span> <span class="mi">0</span>
</code></pre></div></div> <ul> <li>The loop <code class="language-plaintext highlighter-rouge">while j &gt;= 0</code> never runs.</li> <li><code class="language-plaintext highlighter-rouge">nums1</code> stays as <code class="language-plaintext highlighter-rouge">[1, 2, 3]</code> → already correct.</li> </ul> <h3 id="62-nums1-has-no-valid-elements-initially-m--0">6.2. <code class="language-plaintext highlighter-rouge">nums1</code> has no valid elements initially (<code class="language-plaintext highlighter-rouge">m = 0</code>)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums1</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
<span class="n">m</span> <span class="o">=</span> <span class="mi">0</span>
<span class="n">nums2</span> <span class="o">=</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">]</span>
<span class="n">n</span> <span class="o">=</span> <span class="mi">3</span>
</code></pre></div></div> <p>Initial pointers:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i = m - 1 = -1</code></li> <li><code class="language-plaintext highlighter-rouge">j = 2</code></li> <li><code class="language-plaintext highlighter-rouge">k = 2</code></li> </ul> <p>Since <code class="language-plaintext highlighter-rouge">i &lt; 0</code>, the condition <code class="language-plaintext highlighter-rouge">i &gt;= 0 and nums1[i] &gt; nums2[j]</code> is false,<br/> so we always take elements from <code class="language-plaintext highlighter-rouge">nums2</code>:</p> <ul> <li><code class="language-plaintext highlighter-rouge">nums1[2] = 6</code></li> <li><code class="language-plaintext highlighter-rouge">nums1[1] = 5</code></li> <li><code class="language-plaintext highlighter-rouge">nums1[0] = 2</code></li> </ul> <p>Final <code class="language-plaintext highlighter-rouge">nums1 = [2, 5, 6]</code>.</p> <h3 id="63-all-elements-in-nums2-are-smaller">6.3. All elements in <code class="language-plaintext highlighter-rouge">nums2</code> are smaller</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums1</span> <span class="o">=</span> <span class="p">[</span><span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
<span class="n">m</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">nums2</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>
<span class="n">n</span> <span class="o">=</span> <span class="mi">3</span>
</code></pre></div></div> <p>You’ll end up filling <code class="language-plaintext highlighter-rouge">nums1</code> from the back with <code class="language-plaintext highlighter-rouge">[6, 5, 4, 3, 2, 1]</code> in reverse,<br/> but because we’re going from the end and comparing correctly, the final array will still be <code class="language-plaintext highlighter-rouge">[1, 2, 3, 4, 5, 6]</code>.</p> <hr/> <h2 id="7-time-and-space-complexity">7. Time and Space Complexity</h2> <h3 id="71-time-complexity">7.1. Time Complexity</h3> <p>We process each element at most once:</p> <ul> <li>Each iteration moves <code class="language-plaintext highlighter-rouge">k</code> one step to the left.</li> <li>Each iteration also moves either <code class="language-plaintext highlighter-rouge">i</code> or <code class="language-plaintext highlighter-rouge">j</code>.</li> </ul> <p>So the total number of iterations is at most <code class="language-plaintext highlighter-rouge">m + n</code>.</p> <p>$\text{Time Complexity} = O(m + n)$</p> <h3 id="72-space-complexity">7.2. Space Complexity</h3> <p>We only use a few extra integer variables (<code class="language-plaintext highlighter-rouge">i</code>, <code class="language-plaintext highlighter-rouge">j</code>, <code class="language-plaintext highlighter-rouge">k</code>):</p> <ul> <li>No extra arrays</li> <li>No additional data structures</li> </ul> <p>$\text{Space Complexity} = O(1)$</p> <p>This fully satisfies the <strong>in-place</strong> constraint.</p> <hr/> <h2 id="8-common-pitfalls-in-interviews">8. Common Pitfalls in Interviews</h2> <h3 id="81-forgetting-the-in-place-requirement">8.1. Forgetting the In-Place Requirement</h3> <p>A very common wrong answer is:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">result</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span><span class="n">nums1</span><span class="p">[:</span><span class="n">m</span><span class="p">]</span> <span class="o">+</span> <span class="n">nums2</span><span class="p">)</span>
<span class="k">return</span> <span class="n">result</span>
</code></pre></div></div> <p>Even if it “works” in a normal environment, it <strong>fails</strong> this LeetCode problem and typically gets rejected in interviews because:</p> <ul> <li>It uses extra <code class="language-plaintext highlighter-rouge">O(m + n)</code> space.</li> <li>It does not modify <code class="language-plaintext highlighter-rouge">nums1</code> in-place.</li> </ul> <h3 id="82-merging-from-the-front-and-shifting">8.2. Merging from the Front and Shifting</h3> <p>Some candidates try to insert elements from <code class="language-plaintext highlighter-rouge">nums2</code> into the front of <code class="language-plaintext highlighter-rouge">nums1</code> and shift elements to the right one by one.</p> <p>This leads to:</p> <ul> <li>Complicated code</li> <li>Potential bugs with indices</li> <li>Often <code class="language-plaintext highlighter-rouge">O(n^2)</code> time in the worst case</li> </ul> <p>The three-pointer backward merge is cleaner and optimal.</p> <h3 id="83-off-by-one-errors-with-indices">8.3. Off-by-One Errors with Indices</h3> <p>Pay attention to:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i = m - 1</code></li> <li><code class="language-plaintext highlighter-rouge">j = n - 1</code></li> <li><code class="language-plaintext highlighter-rouge">k = m + n - 1</code></li> </ul> <p>Using <code class="language-plaintext highlighter-rouge">m</code>, <code class="language-plaintext highlighter-rouge">n</code>, or <code class="language-plaintext highlighter-rouge">m + n</code> directly as indices is a typical source of bugs.</p> <h3 id="84-returning-the-array">8.4. Returning the Array</h3> <p>This function is <code class="language-plaintext highlighter-rouge">void</code> in many languages (e.g., <code class="language-plaintext highlighter-rouge">void</code> in C++/Java, <code class="language-plaintext highlighter-rouge">None</code> in Python).<br/> The correct behavior is:</p> <ul> <li>Modify <code class="language-plaintext highlighter-rouge">nums1</code> in-place</li> <li>Do <strong>not</strong> return anything</li> </ul> <p>On LeetCode, the platform checks the content of <code class="language-plaintext highlighter-rouge">nums1</code> after the function runs.</p> <hr/> <h2 id="9-interview-takeaways">9. Interview Takeaways</h2> <p>LeetCode 88 is more than just “merge two arrays”:</p> <ul> <li>It’s a textbook example of: <ul> <li><strong>In-place</strong> operations</li> <li><strong>Two/three-pointer</strong> techniques</li> <li>Respecting <strong>time and space</strong> constraints</li> </ul> </li> <li>It trains you to: <ul> <li>Think from the <strong>end</strong> instead of always from the beginning</li> <li>Exploit the structure of the input (sorted arrays + extra space)</li> </ul> </li> </ul> <p>Once you master this pattern, it will help you in many similar problems:</p> <ul> <li>Merging sorted lists</li> <li>Merging intervals</li> <li>Implementing merge sort manually</li> <li>Problems that combine “sorted + extra space at the end”</li> </ul> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="Array"/><category term="TwoPointers"/><category term="Sorting"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Merge Sorted Array]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0027</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0027/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0027"/><published>2026-05-03T14:50:00+00:00</published><updated>2026-05-03T14:50:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0027</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0027/"><![CDATA[<h2 id="problem-0027-remove-element">Problem 0027: Remove Element</h2> <p>Array questions that look simple but have <strong>in-place</strong> and <strong>O(1) space</strong> constraints are classic interview traps.</p> <p>LeetCode 27 – <strong>Remove Element</strong> – is one of those fundamental problems that test whether you truly understand:</p> <ul> <li>In-place array modification</li> <li>The <strong>two-pointer pattern</strong> (fast &amp; slow pointers)</li> <li>How to work under space constraints</li> </ul> <p>In this post, we’ll walk through:</p> <ul> <li>The problem in plain English</li> <li>Why in-place modification matters</li> <li>A clean two-pointer solution</li> <li>A step-by-step walkthrough</li> <li>Time &amp; space complexity</li> <li>Common mistakes and interview tips</li> </ul> <hr/> <h2 id="1-problem-statement-in-plain-english">1. Problem Statement in Plain English</h2> <p>You are given:</p> <ul> <li>An integer array <code class="language-plaintext highlighter-rouge">nums</code></li> <li>An integer value <code class="language-plaintext highlighter-rouge">val</code></li> </ul> <p>Your task:</p> <blockquote> <p><strong>Remove all occurrences of <code class="language-plaintext highlighter-rouge">val</code> from <code class="language-plaintext highlighter-rouge">nums</code>, in-place, and return the number <code class="language-plaintext highlighter-rouge">k</code> of elements that remain.</strong></p> </blockquote> <p>Important details:</p> <ul> <li>You <strong>must</strong> modify <code class="language-plaintext highlighter-rouge">nums</code> in-place, using <strong>O(1) extra space</strong>.</li> <li>The <strong>order of the remaining elements does not matter</strong> according to the problem statement.</li> <li>The judge will only check the <strong>first <code class="language-plaintext highlighter-rouge">k</code> elements</strong> of <code class="language-plaintext highlighter-rouge">nums</code> after your function returns.<br/> Anything after index <code class="language-plaintext highlighter-rouge">k - 1</code> can be ignored.</li> </ul> <h3 id="11-example">1.1. Example</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  nums = [3,2,2,3], val = 3
Output: 2, nums = [2,2,_,_]
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">k = 2</code></li> <li>The first two elements of <code class="language-plaintext highlighter-rouge">nums</code> are <code class="language-plaintext highlighter-rouge">[2, 2]</code>.</li> <li>Values after <code class="language-plaintext highlighter-rouge">k</code> (here marked <code class="language-plaintext highlighter-rouge">_</code>) are irrelevant.</li> </ul> <hr/> <h2 id="2-why-in-place-actually-matters">2. Why “In-Place” Actually Matters</h2> <p>A very natural first idea is:</p> <blockquote> <p>“I’ll just create a new array, copy all elements that are not <code class="language-plaintext highlighter-rouge">val</code>, and return that.”</p> </blockquote> <p>That works logically, but it <strong>violates the constraints</strong>:</p> <ul> <li>Extra space would be <strong>O(n)</strong>, not <strong>O(1)</strong>.</li> <li>Many interview problems (including this one) are deliberately designed to force you to think about: <ul> <li>How to reuse the <strong>same array</strong></li> <li>How to avoid extra allocations</li> <li>How to control <strong>space complexity</strong></li> </ul> </li> </ul> <p>So we need to:</p> <ul> <li>Modify the original <code class="language-plaintext highlighter-rouge">nums</code> directly</li> <li>Avoid any additional data structures like new arrays, lists, or sets</li> </ul> <p>This is where the <strong>two-pointer pattern</strong> becomes really handy.</p> <hr/> <h2 id="3-intuition-fast--slow-pointers">3. Intuition: Fast &amp; Slow Pointers</h2> <p>We want to <strong>filter out</strong> all elements equal to <code class="language-plaintext highlighter-rouge">val</code> while keeping all other elements.</p> <p>Let’s use two indices:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i</code> → <strong>fast pointer</strong>: scans every element in the array</li> <li><code class="language-plaintext highlighter-rouge">k</code> → <strong>slow pointer</strong>: tracks <strong>where</strong> to write the next valid element</li> </ul> <p>Intuition:</p> <ul> <li>Start with <code class="language-plaintext highlighter-rouge">k = 0</code></li> <li>For each index <code class="language-plaintext highlighter-rouge">i</code> from <code class="language-plaintext highlighter-rouge">0</code> to <code class="language-plaintext highlighter-rouge">len(nums) - 1</code>: <ul> <li>If <code class="language-plaintext highlighter-rouge">nums[i]</code> is <strong>not equal</strong> to <code class="language-plaintext highlighter-rouge">val</code>, this element should stay in the array: <ul> <li>Write it at position <code class="language-plaintext highlighter-rouge">k</code>: <code class="language-plaintext highlighter-rouge">nums[k] = nums[i]</code></li> <li>Move <code class="language-plaintext highlighter-rouge">k</code> one step forward (<code class="language-plaintext highlighter-rouge">k += 1</code>)</li> </ul> </li> <li>If <code class="language-plaintext highlighter-rouge">nums[i] == val</code>, we <strong>skip</strong> it (do nothing)</li> </ul> </li> </ul> <p>At the end:</p> <ul> <li>All elements that are <strong>not</strong> equal to <code class="language-plaintext highlighter-rouge">val</code> are packed into <code class="language-plaintext highlighter-rouge">nums[0..k-1]</code></li> <li>The value <code class="language-plaintext highlighter-rouge">k</code> is the number of remaining elements</li> </ul> <p>We’ve effectively <strong>compressed</strong> the array in-place, removing all occurrences of <code class="language-plaintext highlighter-rouge">val</code>.</p> <hr/> <h2 id="4-python-implementation-two-pointer-solution">4. Python Implementation (Two-Pointer Solution)</h2> <p>Here is a clean and optimal Python solution using your idea, polished for readability:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">removeElement</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">nums</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">],</span> <span class="n">val</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Removes all occurrences of val from nums in-place.
        Returns the number of elements that are not equal to val.

        After this function:
        - The first k elements of nums contain the elements != val.
        - The value of k is the return value.
        - Elements after index k - 1 can be ignored.
        </span><span class="sh">"""</span>
        <span class="c1"># k is the "write index" (slow pointer)
</span>        <span class="n">k</span> <span class="o">=</span> <span class="mi">0</span>

        <span class="c1"># i is the "read index" (fast pointer)
</span>        <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">nums</span><span class="p">)):</span>
            <span class="c1"># If current element is not equal to val, keep it
</span>            <span class="k">if</span> <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">!=</span> <span class="n">val</span><span class="p">:</span>
                <span class="n">nums</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>
                <span class="n">k</span> <span class="o">+=</span> <span class="mi">1</span>

        <span class="c1"># k is the number of elements that are not equal to val
</span>        <span class="k">return</span> <span class="n">k</span>
</code></pre></div></div> <p>Your original code is already correct and optimal.<br/> I only added a docstring and some comments to make it more self-explanatory and interview-ready.</p> <h3 id="41-how-to-call-this-method">4.1. How to Call This Method</h3> <p>Since <code class="language-plaintext highlighter-rouge">removeElement</code> is an instance method, we need to create an instance of <code class="language-plaintext highlighter-rouge">Solution</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">3</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">]</span>
    <span class="n">val</span> <span class="o">=</span> <span class="mi">3</span>

    <span class="n">sol</span> <span class="o">=</span> <span class="nc">Solution</span><span class="p">()</span>
    <span class="n">k</span> <span class="o">=</span> <span class="n">sol</span><span class="p">.</span><span class="nf">removeElement</span><span class="p">(</span><span class="n">nums</span><span class="p">,</span> <span class="n">val</span><span class="p">)</span>

    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">k =</span><span class="sh">"</span><span class="p">,</span> <span class="n">k</span><span class="p">)</span>                <span class="c1"># number of remaining elements
</span>    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">result =</span><span class="sh">"</span><span class="p">,</span> <span class="n">nums</span><span class="p">[:</span><span class="n">k</span><span class="p">])</span>    <span class="c1"># first k elements are valid
</span></code></pre></div></div> <p>Possible output:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>k = 4
result = [2, 2, 4, 5]
</code></pre></div></div> <p>Everything after index <code class="language-plaintext highlighter-rouge">k - 1</code> can be ignored by the judge.</p> <hr/> <h2 id="5-step-by-step-walkthrough">5. Step-by-Step Walkthrough</h2> <p>Let’s walk through an example to see how the pointers move.</p> <h3 id="51-example">5.1. Example</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums = [3, 2, 2, 3, 4], val = 3
</code></pre></div></div> <p>Initial state:</p> <ul> <li><code class="language-plaintext highlighter-rouge">k = 0</code> (write index)</li> <li>We iterate <code class="language-plaintext highlighter-rouge">i</code> from <code class="language-plaintext highlighter-rouge">0</code> to <code class="language-plaintext highlighter-rouge">4</code>.</li> </ul> <h4 id="step-1-i--0">Step 1: i = 0</h4> <ul> <li><code class="language-plaintext highlighter-rouge">nums[0] = 3</code></li> <li><code class="language-plaintext highlighter-rouge">nums[0] == val</code> → this element should be removed</li> <li>We do <strong>nothing</strong></li> <li><code class="language-plaintext highlighter-rouge">k</code> remains <code class="language-plaintext highlighter-rouge">0</code></li> <li>Array: <code class="language-plaintext highlighter-rouge">[3, 2, 2, 3, 4]</code></li> </ul> <h4 id="step-2-i--1">Step 2: i = 1</h4> <ul> <li><code class="language-plaintext highlighter-rouge">nums[1] = 2</code></li> <li><code class="language-plaintext highlighter-rouge">nums[1] != val</code> → we want to keep this value</li> <li> <p>Write it at position <code class="language-plaintext highlighter-rouge">k</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="mi">2</span>
<span class="n">k</span> <span class="o">=</span> <span class="mi">1</span>
</code></pre></div> </div> </li> <li>Array becomes: <code class="language-plaintext highlighter-rouge">[2, 2, 2, 3, 4]</code></li> </ul> <h4 id="step-3-i--2">Step 3: i = 2</h4> <ul> <li><code class="language-plaintext highlighter-rouge">nums[2] = 2</code></li> <li>Different from <code class="language-plaintext highlighter-rouge">val</code> → keep it</li> <li> <p>Write at <code class="language-plaintext highlighter-rouge">nums[k]</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="mi">2</span>
<span class="n">k</span> <span class="o">=</span> <span class="mi">2</span>
</code></pre></div> </div> </li> <li>Array: <code class="language-plaintext highlighter-rouge">[2, 2, 2, 3, 4]</code> (effectively the same)</li> </ul> <h4 id="step-4-i--3">Step 4: i = 3</h4> <ul> <li><code class="language-plaintext highlighter-rouge">nums[3] = 3</code></li> <li>Equal to <code class="language-plaintext highlighter-rouge">val</code> → skip</li> <li><code class="language-plaintext highlighter-rouge">k</code> stays at <code class="language-plaintext highlighter-rouge">2</code></li> <li>Array unchanged: <code class="language-plaintext highlighter-rouge">[2, 2, 2, 3, 4]</code></li> </ul> <h4 id="step-5-i--4">Step 5: i = 4</h4> <ul> <li><code class="language-plaintext highlighter-rouge">nums[4] = 4</code></li> <li><code class="language-plaintext highlighter-rouge">nums[4] != val</code> → keep it</li> <li> <p>Write at <code class="language-plaintext highlighter-rouge">nums[k]</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nums</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="mi">4</span>
<span class="n">k</span> <span class="o">=</span> <span class="mi">3</span>
</code></pre></div> </div> </li> <li>Array becomes: <code class="language-plaintext highlighter-rouge">[2, 2, 4, 3, 4]</code></li> </ul> <p>End of loop:</p> <ul> <li><code class="language-plaintext highlighter-rouge">k = 3</code></li> <li>First <code class="language-plaintext highlighter-rouge">k</code> elements: <code class="language-plaintext highlighter-rouge">nums[:3] = [2, 2, 4]</code> → these are the valid ones</li> <li>All occurrences of <code class="language-plaintext highlighter-rouge">3</code> have been removed in-place</li> </ul> <hr/> <h2 id="6-time-and-space-complexity">6. Time and Space Complexity</h2> <h3 id="61-time-complexity">6.1. Time Complexity</h3> <p>We iterate through the array once with a simple <code class="language-plaintext highlighter-rouge">for</code> loop:</p> <ul> <li>Each element is visited exactly once.</li> <li>No nested loops, no recursion.</li> </ul> <p>$\text{Time Complexity} = O(n)$</p> <p>where ( n ) is the length of <code class="language-plaintext highlighter-rouge">nums</code>.</p> <h3 id="62-space-complexity">6.2. Space Complexity</h3> <p>We only use two integer variables:</p> <ul> <li><code class="language-plaintext highlighter-rouge">k</code> (slow pointer)</li> <li><code class="language-plaintext highlighter-rouge">i</code> (fast pointer)</li> </ul> <p>No additional data structures, no extra arrays.</p> <p>$\text{Space Complexity} = O(1)$</p> <p>This satisfies the in-place and constant extra space requirement.</p> <hr/> <h2 id="7-common-mistakes-in-interviews">7. Common Mistakes in Interviews</h2> <h3 id="71-using-extra-arrays-or-lists">7.1. Using Extra Arrays or Lists</h3> <p>For example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">res</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">nums</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">x</span> <span class="o">!=</span> <span class="n">val</span><span class="p">:</span>
        <span class="n">res</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
<span class="k">return</span> <span class="nf">len</span><span class="p">(</span><span class="n">res</span><span class="p">)</span>
</code></pre></div></div> <p>This <strong>breaks the problem constraint</strong>:</p> <ul> <li>Uses extra <code class="language-plaintext highlighter-rouge">O(n)</code> space.</li> <li>Does not modify <code class="language-plaintext highlighter-rouge">nums</code> in-place.</li> </ul> <p>In an interview, this is usually considered <strong>incorrect</strong> for this specific problem.</p> <hr/> <h3 id="72-forgetting-to-return-the-correct-value">7.2. Forgetting to Return the Correct Value</h3> <p>The function must return:</p> <ul> <li>The <strong>number of elements not equal to <code class="language-plaintext highlighter-rouge">val</code></strong> → <code class="language-plaintext highlighter-rouge">k</code></li> </ul> <p>Not the array itself.</p> <p>The judge will:</p> <ul> <li>Look at the first <code class="language-plaintext highlighter-rouge">k</code> elements of <code class="language-plaintext highlighter-rouge">nums</code></li> <li>Ignore the rest</li> </ul> <hr/> <h3 id="73-confusing-what-needs-to-be-removed">7.3. Confusing What Needs to Be “Removed”</h3> <p>We are <strong>not</strong> actually deleting elements in the sense of shrinking the list length in Python.</p> <p>Instead, we:</p> <ul> <li>Overwrite elements in-place</li> <li>Return <code class="language-plaintext highlighter-rouge">k</code> to indicate how many leading elements are valid</li> </ul> <p>Logically, “removed” means:</p> <blockquote> <p>“Not counted and ignored beyond index <code class="language-plaintext highlighter-rouge">k - 1</code>.”</p> </blockquote> <hr/> <h3 id="74-overcomplicating-the-logic">7.4. Overcomplicating the Logic</h3> <p>Some candidates try to:</p> <ul> <li>Use multiple passes</li> <li>Shift elements one-by-one each time they see <code class="language-plaintext highlighter-rouge">val</code></li> </ul> <p>This often leads to:</p> <ul> <li>Longer code</li> <li>Worse performance (e.g., <code class="language-plaintext highlighter-rouge">O(n^2)</code> in naive shifting approaches)</li> </ul> <p>The two-pointer pattern keeps it:</p> <ul> <li>Simple</li> <li>Linear time</li> <li>Easy to reason about</li> </ul> <hr/> <h2 id="8-interview-takeaways">8. Interview Takeaways</h2> <p>LeetCode 27 is more than just removing a value from an array:</p> <ul> <li>It’s a classic example of the <strong>fast &amp; slow pointer</strong> pattern.</li> <li>It reinforces working under <strong>O(1) extra space</strong> constraints.</li> <li>It trains you to think in terms of: <ul> <li><strong>Filtering</strong> in-place</li> <li><strong>Compressing</strong> arrays from left to right</li> </ul> </li> </ul> <p>Once this pattern feels natural, you’ll recognize it in many other problems:</p> <ul> <li>Remove duplicates from sorted array (LeetCode 26)</li> <li>Remove specific elements with additional rules</li> <li>Partitioning arrays based on conditions</li> </ul> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="Array"/><category term="TwoPointers"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Remove Element]]></summary></entry><entry><title type="html">Mastering LeetCode Problem 0026</title><link href="https://mohammadimh76.github.io/blog/2026/leetcode_p0026/" rel="alternate" type="text/html" title="Mastering LeetCode Problem 0026"/><published>2026-05-03T14:10:00+00:00</published><updated>2026-05-03T14:10:00+00:00</updated><id>https://mohammadimh76.github.io/blog/2026/leetcode_p0026</id><content type="html" xml:base="https://mohammadimh76.github.io/blog/2026/leetcode_p0026/"><![CDATA[<h2 id="problem-0026-remove-duplicates-from-sorted-array">Problem 0026: Remove Duplicates from Sorted Array</h2> <p>When preparing for coding interviews, you’ll often see problems that <strong>look trivial</strong> at first glance, but actually test whether you truly understand common patterns like <strong>two pointers</strong>, <strong>in-place modification</strong>, and <strong>space complexity constraints</strong>.</p> <p>LeetCode 26 – <strong>Remove Duplicates from Sorted Array</strong> – is exactly one of those problems.</p> <p>In this post, we’ll walk through:</p> <ul> <li>The problem in plain English</li> <li>Why the <strong>sorted</strong> property is a big deal</li> <li>A clean <strong>two-pointer solution</strong></li> <li>Step-by-step walkthrough with an example</li> <li>Time &amp; space complexity</li> <li>Common mistakes and interview tips</li> </ul> <hr/> <h2 id="1-problem-statement-in-plain-english">1. Problem Statement in Plain English</h2> <p>You are given a <strong>sorted</strong> integer array <code class="language-plaintext highlighter-rouge">nums</code> in non-decreasing order, for example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1, 1, 1, 2, 2, 3, 4, 4]
</code></pre></div></div> <p>Your task:</p> <ol> <li><strong>Remove duplicate elements in-place</strong>, so each unique number appears <strong>only once</strong>.</li> <li>Do this using <strong>O(1) extra space</strong> (you are not allowed to use another array to store the result).</li> <li>Return the <strong>number of unique elements</strong> <code class="language-plaintext highlighter-rouge">k</code>.</li> </ol> <p>Important detail:<br/> The judge will only look at the <strong>first <code class="language-plaintext highlighter-rouge">k</code> elements</strong> of <code class="language-plaintext highlighter-rouge">nums</code>. Anything after index <code class="language-plaintext highlighter-rouge">k - 1</code> can be ignored.</p> <h3 id="11-example">1.1. Example</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  nums = [1,1,2]
Output: 2, nums = [1,2,_]
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">k = 2</code></li> <li>First two elements are <code class="language-plaintext highlighter-rouge">[1, 2]</code></li> <li><code class="language-plaintext highlighter-rouge">_</code> means “don’t care” – values after index <code class="language-plaintext highlighter-rouge">k - 1</code> don’t matter.</li> </ul> <hr/> <h2 id="2-why-the-sorted-property-matters">2. Why the “Sorted” Property Matters</h2> <p>The fact that the array is <strong>sorted</strong> is not just a detail; it’s the cornerstone of the solution.</p> <p>In a sorted array:</p> <ul> <li>All duplicates are grouped together.</li> <li>For any value <code class="language-plaintext highlighter-rouge">x</code>, all its occurrences appear in a <strong>contiguous block</strong>.</li> </ul> <p>That means we can:</p> <ul> <li>Scan from left to right,</li> <li>And simply detect when the <strong>current element is different from the last unique one</strong>.</li> </ul> <p>No hash set, no extra data structures, no random searching.<br/> One forward pass. Two pointers. Done.</p> <hr/> <h2 id="3-intuition-two-pointers-in-action">3. Intuition: Two Pointers in Action</h2> <p>We’ll use a classic <strong>two-pointer technique</strong>:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i</code> → tracks the <strong>position of the last unique element</strong> in the array.</li> <li><code class="language-plaintext highlighter-rouge">j</code> → scans the array from <strong>left to right</strong>.</li> </ul> <p>Initial idea:</p> <ul> <li>Start with <code class="language-plaintext highlighter-rouge">i = 0</code> (the first element is always unique by default).</li> <li>Move <code class="language-plaintext highlighter-rouge">j</code> from index <code class="language-plaintext highlighter-rouge">1</code> to the end: <ul> <li>If <code class="language-plaintext highlighter-rouge">nums[j]</code> is <strong>different</strong> from <code class="language-plaintext highlighter-rouge">nums[i]</code>, we found a <strong>new unique element</strong>: <ul> <li>Move <code class="language-plaintext highlighter-rouge">i</code> one step to the right.</li> <li>Copy <code class="language-plaintext highlighter-rouge">nums[j]</code> into <code class="language-plaintext highlighter-rouge">nums[i]</code>.</li> </ul> </li> </ul> </li> </ul> <p>By the end:</p> <ul> <li>All unique elements are packed at the <strong>start of the array</strong>, from <code class="language-plaintext highlighter-rouge">index 0</code> to <code class="language-plaintext highlighter-rouge">index i</code>.</li> <li>The number of unique elements is <code class="language-plaintext highlighter-rouge">i + 1</code>.</li> </ul> <p>We are effectively <strong>compressing</strong> the array in-place.</p> <hr/> <h2 id="4-the-clean-python-solution">4. The Clean Python Solution</h2> <p>Here is the final implementation in Python:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">removeDuplicates</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">nums</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
        <span class="c1"># If the array is empty, there are no unique elements.
</span>        <span class="k">if</span> <span class="ow">not</span> <span class="n">nums</span><span class="p">:</span>
            <span class="k">return</span> <span class="mi">0</span>
        
        <span class="c1"># i points to the position of the last unique element.
</span>        <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span>
        
        <span class="c1"># j scans through the array starting from index 1.
</span>        <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">nums</span><span class="p">)):</span>
            <span class="c1"># When we find a new value (different from nums[i]),
</span>            <span class="c1"># we move i forward and copy nums[j] into nums[i].
</span>            <span class="k">if</span> <span class="n">nums</span><span class="p">[</span><span class="n">j</span><span class="p">]</span> <span class="o">!=</span> <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]:</span>
                <span class="n">i</span> <span class="o">+=</span> <span class="mi">1</span>
                <span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">j</span><span class="p">]</span>
        
        <span class="c1"># The number of unique elements is i + 1
</span>        <span class="k">return</span> <span class="n">i</span> <span class="o">+</span> <span class="mi">1</span>
</code></pre></div></div> <h3 id="41-how-to-use-it">4.1. How to Use It</h3> <p>Because <code class="language-plaintext highlighter-rouge">removeDuplicates</code> is an <strong>instance method</strong>, you should create an object of <code class="language-plaintext highlighter-rouge">Solution</code> before calling it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">,</span><span class="mi">5</span><span class="p">]</span>
    <span class="n">sol</span> <span class="o">=</span> <span class="nc">Solution</span><span class="p">()</span>
    <span class="n">k</span> <span class="o">=</span> <span class="n">sol</span><span class="p">.</span><span class="nf">removeDuplicates</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span>
    
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">k =</span><span class="sh">"</span><span class="p">,</span> <span class="n">k</span><span class="p">)</span>                   <span class="c1"># number of unique elements
</span>    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">unique part =</span><span class="sh">"</span><span class="p">,</span> <span class="n">nums</span><span class="p">[:</span><span class="n">k</span><span class="p">])</span>  <span class="c1"># only the first k elements matter
</span></code></pre></div></div> <p>Example output:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>k = 5
unique part = [1, 2, 3, 4, 5]
</code></pre></div></div> <hr/> <h2 id="5-step-by-step-walkthrough">5. Step-by-Step Walkthrough</h2> <p>Let’s walk through a concrete example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nums = [1, 1, 1, 2, 2, 3]
</code></pre></div></div> <p>Initial state:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i = 0</code></li> <li><code class="language-plaintext highlighter-rouge">nums[i] = 1</code></li> <li>The first element is always considered unique.</li> </ul> <p>Now we start <code class="language-plaintext highlighter-rouge">j</code> from <code class="language-plaintext highlighter-rouge">1</code>:</p> <ol> <li><code class="language-plaintext highlighter-rouge">j = 1</code> <ul> <li><code class="language-plaintext highlighter-rouge">nums[j] = 1</code>, <code class="language-plaintext highlighter-rouge">nums[i] = 1</code> → same value → duplicate</li> <li>Do nothing.</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">j = 2</code> <ul> <li><code class="language-plaintext highlighter-rouge">nums[2] = 1</code>, still same as <code class="language-plaintext highlighter-rouge">nums[i] = 1</code> → duplicate</li> <li>Do nothing.</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">j = 3</code> <ul> <li><code class="language-plaintext highlighter-rouge">nums[3] = 2</code>, <code class="language-plaintext highlighter-rouge">nums[i] = 1</code> → <strong>different</strong> → new unique value</li> <li>Move <code class="language-plaintext highlighter-rouge">i</code> forward: <code class="language-plaintext highlighter-rouge">i = 1</code></li> <li>Set <code class="language-plaintext highlighter-rouge">nums[1] = 2</code></li> <li>Array becomes: <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1, 2, 1, 2, 2, 3]
</code></pre></div> </div> </li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">j = 4</code> <ul> <li><code class="language-plaintext highlighter-rouge">nums[4] = 2</code>, <code class="language-plaintext highlighter-rouge">nums[i] = 2</code> → duplicate</li> <li>Do nothing.</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">j = 5</code> <ul> <li><code class="language-plaintext highlighter-rouge">nums[5] = 3</code>, <code class="language-plaintext highlighter-rouge">nums[i] = 2</code> → <strong>different</strong> → new unique value</li> <li>Move <code class="language-plaintext highlighter-rouge">i</code> forward: <code class="language-plaintext highlighter-rouge">i = 2</code></li> <li>Set <code class="language-plaintext highlighter-rouge">nums[2] = 3</code></li> <li>Array becomes: <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1, 2, 3, 2, 2, 3]
</code></pre></div> </div> </li> </ul> </li> </ol> <p>End of loop:</p> <ul> <li><code class="language-plaintext highlighter-rouge">i = 2</code></li> <li>Number of unique elements: <code class="language-plaintext highlighter-rouge">k = i + 1 = 3</code></li> <li>Unique prefix: <code class="language-plaintext highlighter-rouge">nums[:3] = [1, 2, 3]</code></li> </ul> <p>Everything after index <code class="language-plaintext highlighter-rouge">2</code> can be ignored.</p> <hr/> <h2 id="6-time-and-space-complexity">6. Time and Space Complexity</h2> <p>This is where the solution really shines in an interview.</p> <h3 id="61-time-complexity">6.1. Time Complexity</h3> <p>We scan the array once with pointer <code class="language-plaintext highlighter-rouge">j</code>.<br/> Each element is visited exactly one time.</p> \[\text{Time Complexity} = O(n)\] <p>where ( n ) is the length of the array.</p> <h3 id="62-space-complexity">6.2. Space Complexity</h3> <p>We don’t use any extra array or data structure.<br/> We only store a couple of integer indices (<code class="language-plaintext highlighter-rouge">i</code>, <code class="language-plaintext highlighter-rouge">j</code>).</p> \[\text{Space Complexity} = O(1)\] <p>This satisfies the requirement of doing the removal <strong>in-place</strong>.</p> <hr/> <h2 id="7-common-mistakes-in-interviews">7. Common Mistakes in Interviews</h2> <p>Here are some mistakes I’ve seen (and made) around this problem:</p> <h3 id="71-using-extra-data-structures">7.1. Using Extra Data Structures</h3> <p>For example:</p> <ul> <li>Using a <code class="language-plaintext highlighter-rouge">set</code> to track seen numbers.</li> <li>Building a new list and returning that.</li> </ul> <p>These might work logically, but they <strong>break the O(1) extra space requirement</strong> and the idea of in-place modification — which is often the main point of the question.</p> <h3 id="72-forgetting-the-edge-case-empty-array">7.2. Forgetting the Edge Case: Empty Array</h3> <p>If <code class="language-plaintext highlighter-rouge">nums</code> is <code class="language-plaintext highlighter-rouge">[]</code>:</p> <ul> <li>Accessing <code class="language-plaintext highlighter-rouge">nums[0]</code> will cause an error.</li> <li>A safe and clean check at the beginning: <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="ow">not</span> <span class="n">nums</span><span class="p">:</span>
    <span class="k">return</span> <span class="mi">0</span>
</code></pre></div> </div> </li> </ul> <h3 id="73-misunderstanding-what-to-return">7.3. Misunderstanding What to Return</h3> <p>The function should return:</p> <ul> <li>The <strong>count</strong> of unique elements <code class="language-plaintext highlighter-rouge">k</code>, <strong>not</strong> the modified array.</li> </ul> <p>The platform will check:</p> <ul> <li>Only the <strong>first <code class="language-plaintext highlighter-rouge">k</code></strong> positions in <code class="language-plaintext highlighter-rouge">nums</code>.</li> </ul> <p>So if your code returns the array instead of <code class="language-plaintext highlighter-rouge">k</code>, it’s wrong in the context of this particular problem.</p> <h3 id="74-incorrect-method-call-in-oop-code">7.4. Incorrect Method Call in OOP Code</h3> <p>In Python, if <code class="language-plaintext highlighter-rouge">removeDuplicates</code> is an instance method:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Solution</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">removeDuplicates</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">nums</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
        <span class="bp">...</span>
</code></pre></div></div> <p>Then calling:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Solution</span><span class="p">.</span><span class="nf">removeDuplicates</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span>
</code></pre></div></div> <p>is incorrect. You need an instance:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">sol</span> <span class="o">=</span> <span class="nc">Solution</span><span class="p">()</span>
<span class="n">sol</span><span class="p">.</span><span class="nf">removeDuplicates</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="8-interview-takeaways">8. Interview Takeaways</h2> <p>This problem is a perfect example of why interviewers love <strong>simple-looking questions</strong>:</p> <ul> <li>It tests whether you recognize the <strong>two-pointer pattern</strong>.</li> <li>It checks if you pay attention to constraints: <ul> <li><strong>in-place</strong>,</li> <li><strong>O(1) extra space</strong>.</li> </ul> </li> <li>It reveals how you handle <strong>edge cases</strong> (empty array, array with one element).</li> <li>It’s a good way to see how clean and readable your code is.</li> </ul> <p>If you can solve this problem smoothly, explain your reasoning clearly, and justify the complexity, you’ll leave a strong impression.</p> <hr/>]]></content><author><name></name></author><category term="LeetCode-Problems"/><category term="Array"/><category term="TwoPointers"/><summary type="html"><![CDATA[LeetCode Daily Challenge_Remove Duplicates from Sorted Array]]></summary></entry></feed>